# 01-{模块名}-需求规格

**模块名**：{模块名}  
**所属系统**：{系统名}  
**文档版本**：v1.0  
**最后更新**：{YYYY-MM-DD}  
**对应页面**：页面[N] [+ 页面[N-A]]

> 本文档供 architect-agent 消费，用于技术逻辑梳理。
> 页面视觉规格见 `02-{模块名}-UI交互规格.md`。

---

## 1. 模块定位

- **所属场景**：场景[Sx]
- **主要角色**：[角色列表]
- **核心功能**：[一句话描述]
- **模块职责**：[本模块在业务流程中的位置和职责]
- **前置模块**：[依赖哪些模块完成]
- **后置模块**：[为哪些模块提供服务]

---

## 2. 功能要点（FR-x）

| FR编码 | 用户故事 | 功能名称 | 功能说明 | 触发条件 | 前置依赖 | 执行顺序 |
|--------|---------|--------|---------|---------|---------|---------|
| FR-1 | 作为[角色]，我希望[...]，以便[...] | [...] | [...] | [...] | 无 | 串行 |
| FR-2 | 作为[角色]，我希望[...]，以便[...] | [...] | [...] | [...] | FR-1 | 串行 |

**字段说明**：
- **用户故事**：绑定本功能的用户故事（句式「作为<角色>，我希望<能力>，以便<价值>」）
- **触发条件**：什么情况下执行本功能
- **前置依赖**：必须先完成哪些FR
- **执行顺序**：串行（必须等前置完成）/ 并行（可同时执行）

---

## 3. 业务实体（本模块涉及）

### 3.1 实体清单

| 实体名 | 业务含义 | 核心属性 | 生命周期 |
|--------|---------|---------|---------|
| [...] | [...] | [...] | [...] |

### 3.2 实体关系

```
实体A 1 --- * 实体B
实体B * --- * 实体C
```

### 3.3 实体详细定义

#### 实体：{实体名}

| 属性名 | 业务类型 | 必填 | 说明 | 默认值 | 来源 |
|--------|---------|------|------|--------|------|
| [...] | [...] | 是 | [...] | — | 用户输入 |

**生命周期**：
- 创建：[何时创建]
- 更新：[何时更新]
- 销毁：[何时销毁]

---

## 4. 业务规则

| BR编码 | 规则名称 | 触发条件 | 判定逻辑 | 成功动作 | 失败动作 | 异常类型 |
|--------|--------|---------|---------|---------|---------|---------|
| BR-1 | [...] | [...] | [...] | [...] | [...] | 业务异常 XXX_ERROR |

**字段说明**：
- **判定逻辑**：如何判断（业务语义，不涉及具体实现）
- **异常类型**：失败时对应的异常编码（供architect映射错误码）

---

## 5. 输入规范

### 5.1 输入参数

| 参数名 | 业务类型 | 必填 | 来源 | 校验规则 | 默认值 | 说明 |
|--------|---------|------|------|---------|--------|------|
| [...] | [...] | 是 | 用户输入 | [...] | — | [...] |

**来源类型说明**：
- 用户输入：前端表单输入
- 系统生成：后端自动生成
- 第三方：调用外部接口获取
- 用户勾选/选择：前端操作触发

---

## 6. 输出规范

### 6.1 成功输出

| 字段名 | 业务类型 | 说明 | 计算规则 |
|--------|---------|------|---------|
| [...] | [...] | [...] | [...] |

**计算规则**：如果字段值需要计算，说明计算方式（业务语义）

### 6.2 失败输出

| 异常场景 | 异常类型 | 用户提示 | 技术说明（供architect映射错误码） |
|---------|---------|---------|------------------------------|
| [...] | [...] | [...] | [...] |

---

## 7. 校验规范

| 校验项 | 校验时机 | 校验规则 | 失败提示 | 阻断级别 |
|-------|---------|---------|---------|---------|
| [...] | 前端 | [...] | [...] | 阻断 |

**字段说明**：
- **校验时机**：前端（用户体验）/ 后端（安全保障）/ 两者（双重校验）
- **阻断级别**：阻断（不通过不能提交）/ 警告（提示但可继续）

---

## 8. 处理流程（关键路径）

> 描述本模块的核心处理流程，帮助architect理解业务逻辑顺序

```
1. 【FR-1 开始】[触发动作]
2. 【校验】[校验项]
3. 【BR-1】[业务规则判断]
   - 成功 → 继续
   - 失败 → [提示] → 【终止】
4. 【输出】[生成结果]
5. 【FR-2】[后续动作]
6. 【终止】[结束状态]
```

**流程说明**：
- 用【】标注关键节点（FR/BR/校验/输出）
- 用 → 表示流转方向
- 标注分支（成功/失败）和终止点

---

## 9. 异常处理

### 9.1 通用异常
详见 `../../../../通用异常处理.md` §1（全局异常）、§2（通用UI状态）

### 9.2 本模块特有异常

| 异常场景 | 异常类型 | 触发条件 | 处理方式 | 降级方案 | 是否重试 |
|---------|---------|---------|---------|---------|---------|
| [...] | 业务异常 | [...] | [...] | — | 否 |

**字段说明**：
- **异常类型**：业务异常（预期内）/ 系统异常（非预期）
- **降级方案**：异常时的备选方案
- **是否重试**：architect判断是否需要自动重试逻辑

---

## 10. 未决项

| 未决项编号 | 问题描述 | 阻塞点 | 影响范围 | 状态 |
|-----------|---------|--------|---------|------|
| OPEN-{模块}-1 | [...] | [...] | [...] | 待确认 |

---

## 附录：变更历史

| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|---------|--------|
| v1.0 | {YYYY-MM-DD} | 初始创建 | prototype-agent |
