# {任务名称} — 接口规划

> 本文档定义对外/对内接口契约。上游：`tech/03-tech-architecture.md`、`tech/04-data-model.md`。下游：编程智能体。
> 统一约定（响应结构 / 错误码 / 鉴权 / 分页）见 `../技术通用规则.md` 第 5 节，本文件只定义具体接口与分段错误码。
> 机器可读优先：每个接口给出 方法 / 路径 / 请求 schema / 响应 schema / 状态码 / 错误码；并须给出 **异常处置** 与 **并发情况需求**，且按**产品页面维度**组织，供门禁 4 逐页人工确认。

## 1. 接口总览
| 资源组 | 接口数 | 说明 |
|--------|-------|------|
| 用户 user | N | 注册/登录/资料 |
| 订单 order | M | 创建/查询/取消 |
（建议同步产出 OpenAPI 3.0 文档，见第 6 节）

## 2. 通用约定（引用）
- 基础路径: [/api/v1]
- 鉴权: [Bearer JWT，Header `Authorization`]
- 统一响应: `{ code, message, data, traceId }`
- 分页: [pageNum/pageSize；响应含 total/pages/list]
- 错误码: [分段规则；本任务使用 2xxxx 用户 / 3xxxx 订单…]

## 3. 按页面维度接口规划

> 接口按**产品页面**分组（页面清单来源 `../prd/02-page-specs.md`）；每个页面列出其**计划接口需求**，逐接口给出 输入 / 输出 / 异常处置 / 并发情况需求，供门禁 4 逐页人工确认。

### 页面：[运动记录页]（来源：prd/02-page-specs.md）
- 页面说明: [展示与录入运动记录]
- 计划接口需求:
  | 接口 | 方法 | 路径 | 说明 |
  |------|------|------|------|
  | 创建运动记录 | POST | /api/v1/records | 录入一条运动 |
  | 查询运动列表 | GET | /api/v1/records | 分页列表 |

#### 接口：创建运动记录 POST /api/v1/records
- 功能: [录入一条运动记录]
- 鉴权: [Bearer JWT]
- 输入（请求 schema）:
  ```json
  { "sportType": "string", "duration": "int", "recordDate": "date" }
  ```
- 输入校验: [sportType 必填；duration > 0]
- 输出（响应 schema）:
  ```json
  { "code": 0, "message": "ok", "data": { "recordId": 1 }, "traceId": "..." }
  ```
- 异常处置:
  | HTTP | code | message | 触发场景 | 处置 |
  |------|------|---------|---------|------|
  | 400 | 20001 | 参数非法 | sportType 空 | 返回错误，不落库 |
  | 401 | 10001 | 未授权 | 缺 token | 要求登录 |
- 并发情况需求: [同一用户并发录入；幂等键 idempotencyKey；峰值 ~X QPS；乐观锁防重复]
- 状态码: [201 创建成功 / 400 参数 / 401 未授权]

#### 接口：查询运动列表 GET /api/v1/records
（每个接口按上述结构展开：功能/鉴权/输入/输出/异常处置/并发情况需求）

### 页面：[...]（下一个页面）
（每个页面按上述结构展开）

## 4. 错误码表（本任务）
| code | HTTP | 含义 | 触发场景 |
|------|------|------|---------|
| 20001 | 409 | 用户名已存在 | 注册重复 |
| 30001 | 400 | 订单金额无效 | ... |

## 5. 接口间依赖与幂等
- [创建订单 → 调用支付；幂等键 orderNo]
- [防重复提交]

## 6. OpenAPI / 导出（可选）
- [本规划应可导出为 OpenAPI 3.0；可在编程阶段由编程智能体直接消费]
