# {系统名} — 系统级技术规范

> 本文件为{系统名}系统的**技术目标与业务约束速查表**——定义本系统在模块、接口、表、性能等方面**需要达到的目标**（业务侧），并提供技术设计导航。
> ⚠️ **技术实现策略不在此定义**：事务隔离级别 / 锁策略 / 幂等实现方案 / 缓存 TTL 与淘汰 / 容量规划手段属**外部技术架构**，由工程基线承载（见 `系统级技术标准.md` §5）；本文件只写**业务侧要求**（判据：换成别的技术栈结论不变）。
> 与本系统配套的**业务约束与跨模块统一约定**（并发容量 / 命名 / 分页 / 鉴权等）见同目录 `系统级技术标准.md`；**数据表统一约定**（系统默认字段 / 软删除过滤）见同目录独立文件 `数据表设计约定.md`；**接口契约**（端点 / schema / 状态码 / 错误码）见同目录独立文件 `接口契约.md`（**唯一权威源**，本文件不重复）；**代码工程结构**（槽位定义 = 「逻辑灌装点位」唯一权威源）见同目录 `代码工程结构说明.md`（**architect-agent 于阶段 1 推导产出，非外部输入**）。
> 详细设计见各模块的 03（后端）/ 04（数据）/ 05（前端）文件。

---

## 1. 系统定位

**系统名**：{系统名}

**业务范围**：{业务范围描述}

---

## 2. 模块清单与指标

| 模块 | 接口数 | 数据表 | 关键 FR | 文件位置 | 状态 |
|------|--------|--------|--------|---------|------|
| {模块名} | {数字} | {数字} | FR-x, FR-y | modules/{模块}/ | ✅/🔄 |

**合计**：{m} 个模块、{i} 个接口、{t} 个表、{f} 个功能点

---

## 3. API 路由前缀

> 本节只约定**业务分域语义**（哪些域归哪些模块），模块级 03 接口路径以此为基础拼接；完整路由与字段命名约定见 `接口契约.md` §0.2 / §1。
> 📌 **接口风格与版本策略属工程基线**：接口风格（RESTful / RPC / GraphQL）、路径命名（kebab-case / 资源名复数）、版本号是否入路径及其形式，一律以工程基线为准（见 `系统级技术标准.md` §5）。

| 范围 | 前缀 | 说明 |
|------|------|------|
| 全局前缀 | {如 /api/v1} | 版本化统一前缀 |
| 鉴权接口 | {如 /api/v1/auth/**} | 登录/登出/刷新 token |
| 业务模块 | {如 /api/v1/{module}/**} | 按模块分域，{module} 取模块英文名 kebab-case |
| 管理后台 | {如 /api/v1/admin/**} | 后台管理接口，需管理员权限 |

**约定**：
- 路由按**模块分域**：每个模块占用一个业务域路径段（如 `/api/v1/{module}/**`），模块间不得互相占用
- 分域路径段与模块英文名的对应关系登记于 `接口契约.md` §0.2（命名风格以工程基线为准）
- 跨模块调用不在本表体现，见 §13「跨模块依赖」

---

## 4. 接口速览

> 📌 **接口清单唯一权威源为 `接口契约.md` §2「接口总表」**——本表**不逐条列举**（避免双源漂移），只保留系统级指标。

| 指标 | 值 |
|------|-----|
| 接口总数 | {i} |
| 按模块分布 | {模块A} {a} 个 / {模块B} {b} 个 |
| 对外 / 内部 | {x} / {y} |
| 接口契约文件 | `接口契约.md`（总表 §2 / 明细 §3 / 错误码 §4） |

---

## 5. 数据表全景

| 表 | 所属模块 | 字段数 | 关键索引 | 特殊设计 |
|------|---------|--------|---------|---------|
| {表名} | {模块} | {数字} | {索引列表} | {特殊说明} |

---

## 6. 核心业务流程

> 只描述**业务上的一次完整操作**（步骤与业务状态流转）；事务边界 / 隔离级别 / 锁策略属外部技术架构（见 `系统级技术标准.md` §5），不在此定义。

### 流程 1：{流程名}

```
{业务流程步骤描述（业务视角：用户动作 → 系统行为 → 结果状态）}
```

---

## 7. 性能目标

> 只写**业务侧性能要求**（用户可感知的响应目标）；缓存 TTL / 淘汰策略 / 锁类型 / 分库分表等手段属外部技术架构，不在此定义。

| 操作 | 目标响应 | 说明 |
|------|---------|------|
| {操作} | {响应时间} | {业务场景} |

---

## 8. 幂等业务要求

> 只写**哪些操作必须防重复提交**（业务要求）；幂等键设计 / 实现方案属外部技术架构，不在此定义。

### 查询接口
- **特性**：天然幂等
- **业务要求**：无

### 提交类接口
- **业务要求**：[列明必须幂等的业务操作，如「创建订单」「支付回调」「批量导入」——重复提交的业务后果是什么]

---

## 9. 缓存业务需求

> 只写「**哪些数据需要缓存、为什么、多重要**」；TTL / 淘汰触发 / 缓存实现属外部技术架构，不在此定义。

| 缓存对象 | 业务原因 | 优先级 |
|---------|---------|--------|
| {缓存对象} | {如：首页高频只读数据} | P0/P1/P2 |

---

## 10. 事务边界（业务视角）

> 只写「**哪些业务动作必须作为一个不可分割的整体成功或失败**」；隔离级别 / 锁策略属外部技术架构，不在此定义。

| 业务 | 涉及表 | 原子性要求 |
|------|--------|-----------|
| {业务} | {表} | {如：扣库存与生成订单必须同时成功或同时失败} |

---

## 11. 权限隔离模式

**核心规则**（业务视角）：

- **数据域隔离**：仅返回当前用户**数据域内**的记录——按归属关系过滤（如「仅本人创建 / 仅本组织归属」），不得跨数据域读取。
- **软删除过滤**：默认排除已软删数据（约定见 `数据表设计约定.md` §2）。

> 📌 过滤条件的**实现方式**（SQL / ORM 拦截 / 框架数据权限）属工程基线，本文件只写业务规则。

---

## 12. 错误码分段规则

> 📌 **错误码清单唯一权威源为 `接口契约.md` §4「错误码总表」**（系统级通用 + 各模块业务汇总）——本节只定**分段规则**，不重复列举错误码；模块 `03` §2「异常处置策略」只写业务处置语义。

| 段 | 范围 | 归属 |
|----|------|------|
| 通用 | {如 1xxxx} | 系统级 |
| {业务域} | {如 2xxxx} | {模块名} |

---

## 13. 跨模块依赖

> 📌 **本节是系统级「模块间依赖关系」的唯一权威源**（**汇总视图**，不新增独立文件）。
> 📌 **逐模块的依赖声明在模块级 `03`**：§1「关联模块」（`FR-x` 粒度）+ §4「跨模块依赖」（模块粒度）。本节**只做汇总与拓扑呈现**，不重复定义模块级内容。

### 13.0 调用形态（按部署形态分叉）

> ⚠️ **部署形态属技术架构，architect 不决策**；本节只按已定形态选择锚点口径。

| 部署形态 | 模块间调用 | 调用锚点 | 与 `接口契约.md` 的关系 |
|---------|-----------|---------|----------------------|
| **单体**（默认） | 本进程调用 | **业务能力**（如「校验客户是否存在」） | **不使用 `operationId`**——本进程调用不产生 HTTP 接口，契约中无对应条目 |
| **已拆服务** | 跨进程调用 | **`operationId`** | 须 ∈ `接口契约.md` §2 接口总表，并有 §3 契约明细 |

> ⚠️ 部署形态未明确时**按单体处理**并登记 `TECH-OPEN-*`；**不得**为满足检查而臆造 `operationId`。

### 13.1 模块依赖矩阵（汇总）

| 调用方模块 | 被调方模块 | 调用内容（业务能力 / `operationId`） | 形态 | 数据流向 |
|-----------|-----------|-----------------------------------|------|---------|
| {模块A} | {模块B} | {业务能力描述} | 单体 / 服务 | {A→B / B→A} |

> 📌 本表 = 各模块 `03` §4 的**汇总**，不承载新增事实；模块 `03` §4 变动时同步刷新。
> 下游消费：编程智能体（跨模块集成）；测试（跨模块链路）。

### 13.2 依赖关系图

```
{模块依赖关系描述}
```

> ⚠️ **不写容错 / 降级方案与同步·异步选型**——两者均属**技术架构**（换栈会变）；业务侧的异常处置语义在模块级 `03` §2「异常处置策略」。

---

## 14. 文件结构与引用

### 目录结构

```
tech/
├── 系统级技术标准.md          ← 业务约束与跨模块统一约定（并发容量/命名/分页/鉴权…；技术架构引用工程基线）
├── 系统级技术规范.md          ← 本文件（模块/接口/表/性能 技术目标）
├── 数据表设计约定.md          ← 数据表统一约定（系统默认字段/软删除过滤；技术细节归工程基线）
├── 接口契约.md                ← 接口契约（端点/schema/状态码/错误码·唯一权威源，03/05/测试均引用）
├── 代码工程结构说明.md        ← 代码工程结构（阶段 1 推导产出：槽位定义含所有权）
├── modules/
│   ├── {模块1}/
│   │   ├── 03-{模块1}-技术规格.md          ← 后端（接口契约/流程/业务逻辑）
│   │   ├── 04-{模块1}-数据模型.md          ← 数据（表/索引/约束）
│   │   └── 05-{模块1}-前端技术规格.md      ← 前端（页面路由/组件树/状态/接口消费/落点）
│   ├── {模块2}/
│   │   ├── 03-{模块2}-技术规格.md
│   │   ├── 04-{模块2}-数据模型.md
│   │   └── 05-{模块2}-前端技术规格.md
│   └── ...
├── tech-系统结果摘要.md
└── tech-系统操作记录.md
```

---

## 15. 自测通过判据（exit criteria）

> 第三环（编程智能体）「全量生成 + 自测」的**验收判据**。分层判定：**L1 / L2 阻断，L3 告警不阻断**。
> ⚠️ **物理边界**：可自动验证与需人工确认的部分必须**分列**——移动端小程序无法纯 CI 自测、第三方集成需真实凭证、视觉还原需人工比对，**不得一刀切「全绿」**（否则永远不达标）。

**可运行性元数据（执行前置 · 登记项）**

> 第三环自动编排「生成 → 构建 → 测试」的**唯一依据**——无构建命令 / 测试命令 / 启动顺序 / 端口，则无法自动跑通自测。
> 📌 属**工程配置常量**（换技术栈会变）：本表只**登记**（读自工程仓库配置 / 脚手架 skill 的 `assets/scaffold/`），**不定义**；缺失登记 `TECH-OPEN-*`，**不得臆造**。
> 📌 与 `代码工程结构说明.md`（槽位表）**同轮于阶段 1 登记**。

| 工程 | 构建命令 | 测试命令 | 测试框架 | 启动顺序 | 端口 |
|------|---------|---------|---------|---------|------|
| {工程名} | {如 mvn -q -DskipTests package} | {如 mvn test} | {如 JUnit 5} | {1 / 2} | {如 8080} |

**通过判据（分层 · L1 / L2 阻断，L3 告警）**

| 层 | 判据 | 是否阻断 | 判据来源 |
|----|------|---------|---------|
| L1 契约测试 | 契约测试全绿（接口边界 / 参数边界 / 错误码） | **阻断** | `接口契约.md` §2 / §3 / §4 |
| L2 主链路 E2E | 关键业务链路 E2E 全绿 | **阻断** | 本文件 §6 核心业务流程 |
| L3 覆盖率 | 行 / 分支覆盖率 ≥ 阈值 | 告警不阻断 | 工程基线 |
| 例外清单 | 不可自动验证项单列「需人工确认」 | 不阻断但**必须登记** | 本文件「本系统不可自动验证项」|

**分界原则（契约测试 vs E2E）**：**接口层用契约测试**（不需要真实业务链，可独立跑）；**业务层用 E2E**（需后端在运行）。

**本系统不可自动验证项**：

| 项 | 原因 | 人工确认方式 |
|----|------|-------------|
| {如：移动端小程序交互} | {需微信开发者工具 / 真机} | {人工验收清单} |

**测试工具与框架**：属**工程基线**（见上「可运行性元数据」表「测试框架」列），本文件不定义。

---

## 16. 版本记录

| 时间 | 变更内容 | 变更人 |
|------|---------|--------|
| YYYYMMDD | 初版：系统级技术目标定义 | architect-agent |
