# {系统名} — 代码工程结构说明

> 📌 **本文件由 architect-agent 在阶段 1 产出**——**本项目产物，不是外部输入**（不存在独立的外部供给方；早期设计曾假设「工程侧交付」，该定位已废弃）。
> 📌 **生成方式 = 推导优先 + 差额问询**：
> - **零提问推导**：端范围（`../prd/系统要求规范.md` §6.3）→ 需要哪些工程；技术基线（`系统级技术标准.md` §5）+ **已有脚手架 skill 的 `assets/scaffold/`**（如 `uni-app-scaffold`）→ 标准目录骨架；模块划分（**阶段 1 的「模块初步归纳草案」**〔`module → FR-x` 映射〕；后续迭代可与 `modules/{模块}/03-技术规格.md` §1 模块脊柱对齐）→ 槽位数量与命名。
> - **差额问询**：只问**推不出**的——既有代码位置 / 自定义分层 / 人工保护区。**能推的不问。**
> 📌 **推导前提（本文件成立的基础）**：**技术栈一旦确定，标准目录骨架即已知常量**（结构跟着技术栈走）。故本文件**以推导为主、问询为辅**，不是一份需要谁来填写的问卷。
> 📌 **唯一用途**：回答「逻辑要灌到哪里」。`modules/{模块}/03-技术规格.md` §3 每个 `LU-x` 的「落点」与 `modules/{模块}/05-前端技术规格.md` §8「前端落点」**必须**落在下表声明的槽位内。
> 📌 **可运行性元数据**（构建命令 / 测试命令 / 测试框架 / 启动顺序 / 端口）**不在本文件**，见 `系统级技术规范.md` §15。

---

## 1. 槽位定义（哪些位置留给业务逻辑）

> 本表是「逻辑灌装点位」的**唯一权威源**。
> 📌 **不另列目录骨架**——表内「路径」列的**并集即目录骨架**，两处描述同一事实只会造成漂移。
> 📌 **所有权**（第三环重跑规则）：`AI 生成` = 可由 AI 覆盖重生成；`人工维护` = AI 重跑**跳过**；`AI 生成后接管` = 首次 AI 生成、之后人工，重跑**跳过**。
> 📌 槽位由推导得到；自定义分层 / 既有代码位置经问询后登记；仍不确定的填 `TECH-OPEN-*`，**不得臆造路径**。

| 槽位ID | 工程 | 路径 | 类型 | 所有权 |
|--------|------|------|------|--------|
| SL-1 | {工程名} | {目录 / 文件} | 控制器 / 服务 / 仓库 / 实体 / 页面 / 组件 / 路由 / 状态 / 测试 | AI 生成 / 人工维护 / AI 生成后接管 |
