# {系统名} — 系统级技术标准

> 本文件为**{系统名}系统**的**业务约束与跨模块统一约定**权威源，只承载「换任何技术栈结论都不变」的内容：
>   并发与容量目标（业务量级）/ 命名与规范约定 / 跨模块统一约定 / 通用领域模型 / 技术基线引用。
> ⚠️ **技术架构不在此定义**：编程语言 / 框架中间件 / 系统分层 / 部署形态 / 非功能性技术设计属**外部技术架构**，由工程基线（工程仓库的构建与部署配置文件、CI 配置、代码风格配置）承载，本文件只作**引用声明**（见 §5）。
> 📌 **判据（唯一）**：**换成别的技术栈，结论会不会变？** 会变 → 外部技术架构 / 技术策略 / 技术配置，本文件不产出；不变 → 业务语义 / 跨模块统一约定，必须写。
> **接口契约**（端点 / schema / 状态码 / 错误码）的唯一权威源是独立文件 `接口契约.md`，本文件不承载。
> 与本系统配套的**技术目标**（模块 / 接口 / 表 / 性能指标速查）见同目录 `系统级技术规范.md`，二者职责分离：**标准 = 业务约束与约定，规范 = 技术目标**。
> **数据表设计约定**不并入本标准，见同目录独立文件 `数据表设计约定.md`——只承载系统默认字段与软删除策略（数据技术细节属工程基线）。
> 产品侧通用规则见项目级 `通用异常处理.md` / `通用校验规则.md`（角色 / 权限见 `系统要求规范.md`、UI 标准见 `系统交互规范.md`、术语见 `项目级产品摘要.md`）。本文件聚焦**本系统**的业务约束；**不设项目级技术通用规则**。

---

## 1. 并发与容量目标（业务约束，须与用户确认）
> 本节只写**业务侧要求**（换技术栈结论不变）。具体技术手段（分库分表 / 冷热分离 / 缓存方案 / 限流实现）属外部技术架构，见 §5。

- **日活 / 数据量级**: [如 DAU 5w；核心表年增量 ~500w 行]
- **峰值 QPS / TPS**: [如读 2000 QPS / 写 500 TPS；或「待确认」]
- **一致性要求**: [强一致（支付/库存）/ 最终一致（聚合统计）/ 混合]
- **延迟要求**: [如 P99 < 200ms；批量导入任务 < 5min]
- **可用性目标**: [如 99.9%；单实例起步可降级]

## 2. 命名与规范约定
> 只写**跨技术栈稳定**的命名与组织约定；代码风格 / 提交规范 / 构建配置属工程基线（见 §5）。
- **数据库命名**: 表名 / 字段名 snake_case；主键 id；必须含 created_at / updated_at；软删字段 deleted_at（详见「数据表设计约定.md」）
- **接口字段命名映射**: 契约层命名映射（snake_case ↔ lowerCamelCase）、响应包络与分页详见「接口契约.md」§0.2 / §1.1 / §1.2
- **接口风格与路由策略**: 接口风格（RESTful / RPC / GraphQL）、路径命名（kebab-case / 资源名复数）、路由前缀与版本号策略**属工程基线**（见 §5）；本系统只约定**业务分域语义**（统一前缀 + 按模块分域，见「系统级技术规范.md」§3）

## 3. 跨模块统一约定
> 系统内所有模块共用的业务约定；模块级 03/04/05 只引用不重复。
- **状态码约定**: [HTTP 状态码使用约定；业务状态码枚举见「接口契约.md」]
- **分页约定**: 统一 pageNum / pageSize；返回 total / pages / list（详见「接口契约.md」§1.2）
- **排序 / 过滤约定**: 统一 query 参数约定
- **鉴权约定**: 需鉴权接口统一在 Header 携带凭证（{如 Authorization: Bearer <token>}）；鉴权方案选型（JWT / Session / 网关）属工程基线；权限隔离模式见「系统级技术规范.md」§11
- **软删除约定**: 查询默认过滤 `deleted_at IS NULL`（详见「数据表设计约定.md」）
- **审计字段约定**: created_by / created_at / updated_by / updated_at 全员约定（详见「数据表设计约定.md」）
- **多租户约定**: [如有：tenant_id 行级隔离约定]
- **幂等业务要求**: [哪些操作必须防重复提交，如「创建订单」「支付回调」「批量导入」；幂等键设计与实现方案属工程基线，不在此定义]

## 4. 通用领域模型（可选，跨模块复用）
- [在本系统各模块中反复出现的业务概念，如：用户、组织、附件；在此定义通用业务属性与语义，模块级 04 只引用]

## 5. 技术基线引用（外部技术架构 · 本文件不定义）
> 以下内容属**外部技术架构**，由工程基线（工程仓库的构建 / 部署配置文件、CI、代码风格配置）承载；本文件与模块级 03/04/05 一律**引用不定义**。
> ⚠️ 若工程基线尚未建立，登记 `TECH-OPEN-*` 并在门禁中提出，**不得由本 agent 臆造技术选型**。

| 项 | 承载方 | 本系统引用备注 |
|----|--------|--------------|
| 编程语言与版本 | 工程基线 | [如 后端服务脚手架] |
| 核心框架与中间件 | 工程基线 | [引用工程基线] |
| 系统分层与模块划分 | 工程基线 | [遵循工程基线分层] |
| 接口风格与路由策略（RESTful / RPC / GraphQL · 路径命名 · 版本号策略） | 工程基线 | [遵循工程基线接口规范] |
| 部署形态与拓扑 | 工程基线 | [遵循工程基线部署方案] |
| 非功能性技术设计（性能手段 / 安全实现 / 可观测 / 高可用 / 容量规划） | 工程基线 | [遵循工程基线] |
| 测试与验收判据（测试框架 / 工具链） | 工程基线 | [判据**分层**见 `系统级技术规范.md` §15「自测通过判据」；工具与框架遵循工程基线] |

## 6. 本规则文件变更记录
> 仅记录**本规则文件自身**的增删改（如新增统一约定），与「项目级技术摘要.md §2 项目维护记录」（项目下技术系统的修改历史）区分，避免混淆。

| 时间 | 来源系统 | 更新内容 |
|------|---------|---------|
| {YYYYMMDD} | {系统名} | 初始化本文件（定位于**业务约束与跨模块统一约定**；技术架构改为引用工程基线，不再由本文件定义） |
| {YYYYMMDD} | {系统名} | §5 新增「测试与验收判据（测试框架 / 工具链）」引用行（判据分层见 `系统级技术规范.md` §15） |
| […] | […] | […] |
