# {模块名称} — 技术规格说明书

> 📌 **定位**：需求翻译器 / 中转站（architect-agent 模块级产物）。
> 📌 **版本**：v1.0

上游：`../../prd/modules/{模块名}/01-{模块名}-需求规格.md` | 下游：`04-{模块名}-数据模型.md`

---

## 0. 架构约束引用

- **系统标准**：`../系统级技术标准.md`（语言/框架/分层/部署/并发基线，仅引用）
- **系统规范**：`../系统级技术规范.md`（路由前缀、幂等性、缓存、权限隔离、错误码等技术目标，仅引用）
- **本模块只补差异**：只写本模块特有的接口、流程、业务规则

---

## 1. 模块定义与功能点映射

> 本段为「模块 → 功能点」两层结构的落地：先定义模块边界，再映射功能点（FR-x → TFP-x → 接口）。

### 1.1 模块定义 / 边界段

- **核心业务对象 / 能力**：{本模块围绕的核心业务对象或能力}
- **统一业务逻辑（内聚点）**：{将多个 FR-x 聚合为本模块的统一业务逻辑}
- **聚合的 FR-x**：FR-x、FR-y、…
- **关联场景**：场景[Sx]（经 FR-x，来自 系统要求规范 §2）
- **关联模块**：{跨模块调用时被调用/调用的模块}
- **边界说明**：本模块不含 {…}（明确不归本模块的内容）

### 1.2 功能点映射表（FR → TFP）

| 需求 FR | 技术功能点 TFP | 对外/内部 | 对应接口 | 说明 |
|---------|---------------|----------|---------|------|
| FR-x | TFP-x.1 | 对外/内部 | 接口 1 | {对应说明} |
| FR-x | TFP-x.2 | 对外/内部 | 接口 2 | {对应说明} |

> 每条 FR-x 必须且仅归属一个主模块；跨模块调用在「关联模块」记录，不重复归属。
> FR-x 为模块内编号（从 FR-1 起编），全局以「模块名+FR-x」组合定位；本表「需求 FR」列填写本模块的 FR-x。

---

## 2. 对外接口清单

> 每个接口内联完整要素（输入 / 校验 / 处理逻辑伪代码 / 输出 schema / 异常 / 关联数据），
> 满足「机器可读（schema + 状态码 + 错误码） + 伪代码可译」双诉求；不写具体框架 / ORM。

### 接口 1：{接口名称}

- **端点**：{方法} {路径}
- **对外**：✅ 对外 / ❌ 内部
- **功能描述**：{一句话说明接口用途}

**输入（请求 schema）**：
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| {字段} | {类型} | 是/否 | {说明} |

**校验**：
- {校验规则 1}
- {校验规则 2}

**处理逻辑（伪代码）**：
```
{逻辑流程，不写具体框架/ORM}
```

**输出（成功响应 schema）**：
| 字段 | 类型 | 说明 |
|------|------|------|
| {字段} | {类型} | {说明} |

**异常**：
| 错误码 | HTTP | 触发条件 | 用户提示 |
|--------|------|---------|---------|
| {错误码} | {状态码} | {触发条件} | {提示语义} |

**关联数据 → 04**：{实体名}（详见 `04-{模块名}-数据模型.md`）

---

### 接口 2：{接口名称}

（同接口 1 结构，逐接口展开）

---

## 3. 关键业务流程

### 流程 1：{流程名}

```
{流程图或流程描述（伪代码），标注状态机/算法分支}
```

---

## 4. 跨模块依赖

| 依赖关系 | 说明 |
|---------|------|
| 本模块对外提供 | {接口清单} |
| 本模块依赖 | {依赖模块} |
| 数据流向 | {数据流描述} |

**详见**：`../模块间接口依赖清单.md`

---

## 5. 版本记录

| 版本 | 日期 | 变更 | 变更人 |
|------|------|------|--------|
| v1.0 | YYYYMMDD | 初版 | architect-agent |
