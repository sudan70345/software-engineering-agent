# {模块名称} — 数据模型说明书

> 📌 **定位**：业务实体、表结构、查询场景与索引。
> 📌 **版本**：v1.0

上游：`03-{模块名}-技术规格.md` | 下游：编程智能体（代码实现）

---

## 0. 表设计约定引用

- **系统表约定**：`../../数据表设计约定.md`
- **所有表自动包含系统默认字段**：id, created_by, created_at, updated_by, updated_at, deleted_at

---

## 1. 实体与表清单

| 实体 | 表名 | 核心字段 | 说明 |
|------|------|--------|------|
| {实体名} | {table_name} | {字段列表} | {说明} |

---

## 2. 建表脚本

### 表 1：{table_name}

```sql
CREATE TABLE IF NOT EXISTS {table_name} (
  -- 系统字段（来自 数据表设计约定.md）
  id BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  created_by VARCHAR(100) COMMENT '创建人',
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  updated_by VARCHAR(100) COMMENT '修改人',
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后修改时间',
  deleted_at DATETIME COMMENT '逻辑删除时间',
  
  -- 业务字段
  {业务字段定义},
  
  PRIMARY KEY (id),
  KEY idx_{table_name}_{field} ({field})
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='{表中文描述}';
```

---

## 3. 查询场景 → 索引映射

| 场景 | 表 | 查询条件 | 索引 | 优先级 |
|------|-----|---------|------|--------|
| {场景描述} | {表名} | {WHERE 条件} | {索引建议} | P0/P1/P2 |

---

## 4. 并发场景与锁策略

{说明本模块涉及的并发保护策略：行级锁、事务隔离级别、唯一约束等}

---

## 5. 数据一致性约束

| 表 | 字段 | 约束类型 | 说明 |
|------|------|---------|------|
| {表名} | {字段} | PRIMARY KEY / FOREIGN KEY / UNIQUE / NOT NULL | {说明} |

---

## 6. 版本记录

| 版本 | 日期 | 变更 | 变更人 |
|------|------|------|--------|
| v1.0 | YYYYMMDD | 初版 | architect-agent |
