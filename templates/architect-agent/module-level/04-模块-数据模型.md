# {模块名称} — 数据模型说明书

> 📌 **定位**：业务实体、表结构、查询场景与索引。
> 📌 **版本**：v1.0

上游：`03-{模块名}-技术规格.md` | 下游：编程智能体（代码实现）

---

## 0. 表设计约定引用

- **系统表约定**：`../../数据表设计约定.md`（系统默认字段 + 软删除过滤；字段类型 / 字符集 / 引擎 / 索引名以**工程基线**为准，本文件不定义）
- **所有表自动包含系统默认字段**：id, created_by, created_at, updated_by, updated_at, deleted_at

---

## 1. 实体与表清单

| 实体 | 表名 | 核心字段 | 说明 |
|------|------|--------|------|
| {实体名} | {table_name} | {字段列表} | {说明} |

---

## 2. 建表脚本

### 表 1：{table_name}

```sql
CREATE TABLE IF NOT EXISTS {table_name} (
  -- 系统字段（来自 数据表设计约定.md）
  id BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  created_by VARCHAR(100) COMMENT '创建人',
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  updated_by VARCHAR(100) COMMENT '修改人',
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '最后修改时间',
  deleted_at DATETIME COMMENT '逻辑删除时间',

  -- 业务字段
  {业务字段定义},

  PRIMARY KEY (id),
  KEY idx_{table_name}_{field} ({field})
) COMMENT='{表中文描述}';
```

> 📌 上例中字段类型 / 字符集 / 存储引擎 / 索引命名仅为示意，**以工程基线为准**（属外部技术架构，不由本 agent 定义）。

---

## 3. 查询场景 → 索引映射

> 来源：本模块 `03-{模块名}-技术规格.md` §2 各**读接口**的「查询语义」（筛选维度 / 默认排序 / 过滤条件）。
> ⚠️ 两者须**一一对齐**：03 查询语义定「业务上按什么查、怎么排」，本节定「用哪个索引落点」——03 每个筛选维度 / 排序字段在本表须有对应行（**无遗漏**）；本表每行须能回溯到某个读接口（**无凭空场景**）。
> 本节**只写索引落点与优先级**，不写访问技术方案（ORM / 分页插件 / 二级缓存 / 读写分离 / 连接池 / 执行计划提示）。

| 场景 | 表 | 查询条件 | 索引 | 优先级 |
|------|-----|---------|------|--------|
| {场景描述}（来源：03 §2 接口N / `{operationId}`） | {表名} | {WHERE 条件（仅用于确定索引列）} | {索引建议} | P0/P1/P2 |

---

## 4. 并发业务场景

> 只写**业务视角的并发冲突**：哪些操作会并发冲突、业务后果是什么、期望的业务约束是什么。
> ⚠️ 具体锁类型 / 隔离级别 / 事务边界 / 唯一约束实现属**外部技术架构**（见 `../../系统级技术标准.md` §5），不在此定义。

| 并发场景 | 冲突操作 | 业务后果 | 期望业务约束 |
|---------|---------|---------|-------------|
| {如：同一商品并发下单} | {下单 × 下单} | {超卖} | {库存不得为负} |

---

## 5. 数据一致性约束

| 表 | 字段 | 约束类型 | 说明 |
|------|------|---------|------|
| {表名} | {字段} | PRIMARY KEY / FOREIGN KEY / UNIQUE / NOT NULL | {说明} |

> 📌 本节只声明**业务约束**（唯一性 / 必填 / 引用关系的业务含义）；约束的数据库实现方式以工程基线为准。

---

## 6. 版本记录

| 版本 | 日期 | 变更 | 变更人 |
|------|------|------|--------|
| v1.0 | YYYYMMDD | 初版 | architect-agent |
