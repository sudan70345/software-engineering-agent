# {模块名} — 前端技术规格说明书

> 📌 **定位**：需求翻译器 / 中转站（architect-agent 模块级产物 · 前端侧）。
> 📌 **版本**：v1.0
> 📌 **本文件承载「前端要做什么」，不承载「前端怎么搭」**：框架选型、路由注册方式、请求层封装、状态库选型、构建配置均属前端工程结构常量，由工程配置文件约束，本文件不写。

上游：`../../../prd/modules/{模块名}/01-{模块名}-需求规格.md`（规则 / 校验分工） + `02-{模块名}-UI交互规格.md`（页面 / 交互 / 状态）
下游：`编程智能体`（Web 前端工程 / 移动端工程）

---

## 0. 前端约束引用

- **接口契约**：`../../接口契约.md`（**端点 / 请求响应 schema / 状态码 / 错误码的唯一权威源**）——本文档**只引用 operationId，不定义接口**；同模块 `03-{模块名}-技术规格.md` §2 为**后端业务上下文**（功能描述 / 校验语义 / 处理逻辑 / 查询语义 / 异常处置），本文档只取其中的 operationId 与业务结果语义，不读技术实现细节
- **系统级技术规范**：`../../系统级技术规范.md`（路由前缀、权限隔离模式、错误码，仅引用）
- **端范围**：`../../../prd/系统要求规范.md` §6.3「端范围」（本期覆盖 Web / 移动端 / 双端）
- **布局转译规则（与 `02` 的分工，唯一口径）**：`02-{模块名}-UI交互规格.md` 只写**布局语义**（区域 / 定位方式 / 尺寸档位），**不写画布、坐标与 px**；本文档负责把语义**转译**为布局落点。**两侧不得互换**：

| 02 侧语义（来源） | 本文档落点（转译目标） | 取值出处 |
|------------------|---------------------|---------|
| 端形态（02 §2） | §7「平台差异」+ 断点适配声明 | `../../../prd/系统要求规范.md` §6.3 |
| 基准视口宽度区间（02 §2） | §7 断点档位（不写具体数值） | `../../../prd/界面设计要素.md`「断点」 |
| 区域 + 定位方式（02 §3.2） | §1 / §2 布局容器落点（固定顶栏 / 固定侧栏 / 自适应主区） | 本文档 §1 / §2 自行声明 |
| 区域尺寸语义：固定档位 / 自适应（02 §3.2） | 尺寸令牌引用 | `../../../prd/界面设计要素.md`「布局与尺寸档位」 |
| 组件尺寸档位（02 §4.1 / §4.2） | §2 组件树 + 令牌引用 | `../../../prd/系统交互规范.md` §1.3 / `界面设计要素.md` |

> ⚠️ **本文档不得出现具体 px 与坐标**，也不得反推画布尺寸；尺寸一律引用令牌语义名（见 §0 末条边界）。
> 📌 **区域覆盖要求**：02 §3.2 的**每个区域**须在本文档 §1 / §2 有对应布局落点（**无遗漏、无凭空新增**），由 `skills/self-check` 检查 4e 校验。
- **工程结构**：`../../代码工程结构说明.md`（**阶段 1 推导产出**：**§1 槽位定义（含所有权）**）——§8「前端落点」**必须落在其 §1 槽位内**；槽位未定义时登记 `TECH-OPEN-*`，**不臆造目录**
- **本模块只补差异**：只写本模块特有的页面、组件、状态、接口消费

---

## 1. 页面与路由

> 来源：`02` §1 页面定位 / §7 页面流转 + `系统要求规范.md` §3 角色权限。
> ⚠️ **本表须与 `02` §1 一一对应**：02 的每个 页面[N]/[N-A] 都要有一行；编号与 02 完全一致，不得重编号、不得凭空新增。

| 页面 | 路由 path | 页面类型 | 可见角色 | 进入参数 | 路由守卫 | 对应 FR |
|------|----------|---------|---------|---------|---------|--------|
| 页面[N] | /{path} | 主页面 / 辅助页面 | {角色} | {参数名} | {守卫条件，无则填「无」} | FR-x |
| 页面[N-A] | /{path} | 辅助页面（{所属主页面}） | {角色} | {参数名} | {守卫条件} | FR-x |

**页面[N-A] 呈现方式**：☑ 独立路由 / ☐ 局部组件（弹窗）—— 复杂弹窗走独立路由，简单弹窗走局部组件。
> ⚠️ **落点收口**：本表每个页面须在 §8「前端落点」有对应行，不得留空槽位。

---

## 2. 组件树

> 来源：`02` §4 功能组件清单（**补充父子层次与复用关系**）。
> `02` 是平铺清单（面向绘制），本节是树（面向实现）。

| 组件ID | 组件名 | 父级 / 所属区域 | 对应 02 组件 | 复用范围 | 对应 FR |
|--------|-------|---------------|-------------|---------|--------|
| FC-1 | {组件名} | 页面[N] {区域} | C1 | 本页 | FR-x |
| FC-2 | {组件名} | FC-1 | C2 | 跨页 | FR-x |

**跨页复用组件清单**（在模块内首次出现处定义，其余页面引用）：

| 复用组件 | 复用页面 | 复用理由 |
|---------|---------|---------|
| {组件名} | 页面[N]、页面[M] | {共同业务语义} |

---

## 3. 前端状态定义

> **本文档新增**（`02` 无此信息）。说明页面需要维护哪些状态、作用域与来源。
> 不写状态库选型（属工程结构常量）。

| 状态名 | 作用域 | 初始值 | 变更来源 | 持久化 | 对应 FR |
|--------|-------|--------|---------|--------|--------|
| {状态名} | 页面 / 全局 / 服务端缓存 | {初始值} | 用户操作 / 接口响应 | 否 / 本地 | FR-x |

**作用域判定说明**：
- **页面**：仅本页面使用，离开即销毁
- **全局**：跨页面共享（如当前登录用户、数据域），需在 §0 系统级声明
- **服务端缓存**：来自接口、需在多个页面复用同一份数据（须声明失效时机）

---

## 4. 接口消费映射

> 来源：`02` §5 关键交互行为 + **`../../接口契约.md`**（operationId / 错误码）。
> **本节不定义接口**（接口定义唯一在 `../../接口契约.md`），只声明「何时调、怎么用、失败了怎么呈现」。

| 页面 / 组件 | 触发时机 | operationId | 请求参数来源 | 响应字段用途 | 错误码 → 前端呈现 |
|------------|---------|------------|------------|-------------|-----------------|
| 页面[N] | 进入页面 / 提交表单 / … | {operationId} | {表单字段 / 路由参数 / 页面状态} | {用于渲染哪个区域} | {错误码} → {呈现方式} |

> ⚠️ `operationId` 须在 `../../接口契约.md` §2 接口总表存在且 §3 有对应契约明细（**无悬空引用**）；错误码须 ∈ 该接口契约明细「状态码 / 错误码」表的集合（**无凭空错误码**）。
> 本表**只声明消费方式**，不得出现 schema / 状态码 / 错误码定义。

**请求时机说明**：
- 进入页面即请求 / 用户操作触发 / 前置请求成功后再请求（串行）
- 是否需要防抖 / 是否需要取消上一次请求（如搜索）

---

## 5. 前端校验

> 来源：`01` §7 校验规范（**只取「校验时机 = 前端 / 两者」的项**）。
> 后端校验（校验时机 = 后端）不在本节，属 `03` 职责。

| 校验项 | 字段 | 触发时机 | 校验规则 | 失败提示 |
|-------|------|---------|---------|---------|
| {校验项} | {字段名} | blur / change / submit | {规则} | {提示文案} |

**注**：失败提示文案语义来自 `01`；视觉呈现（Toast / 行内 / 弹窗）来自 `02` 与 `通用异常处理.md`。

---

## 6. 页面状态逻辑

> 来源：`02` §6 页面状态变体 + 项目级 `通用异常处理.md` §2。
> `02` 描述「看起来是什么样」，本节描述「何时进入、如何恢复」。

| 状态 | 触发条件 | 展现要点 | 恢复动作 |
|------|---------|---------|---------|
| 初始态 | 页面加载完成 | {要点} | — |
| 加载中 | {触发} | {要点} | — |
| 空态 | {触发} | {要点} | {引导动作，如「新建」} |
| 错误态 | {触发} | {要点} | {重试 / 返回} |
| 受限态 | {触发，如无权限} | {要点} | {申请 / 返回} |

> 通用状态（空态 / 加载态 / 网络错误）细节见 `通用异常处理.md` §2，本节只写本模块的进入条件与恢复动作。

---

## 7. 平台差异

> **只写本模块页面特有的端差异**。
> 全端统一的能力差异（路由模型、返回、扫码、文件上传等）由系统级声明；端范围由 `系统要求规范.md` §6 决定。
> 若本期端范围为单端，本节填「不适用」。

| 项 | Web | 移动端 | 说明 |
|----|-----|--------|------|
| {页面元素 / 功能} | {表现} | {表现} | {差异原因与处理} |

---

## 8. 前端落点

> 结构由前端工程配置文件约束（**常量**），本文档只声明「逻辑灌到哪个位置」。
> 不写框架、目录结构设计、构建配置。
> ⚠️ **落点须落在 `../../代码工程结构说明.md` §1「槽位定义」声明的槽位内**（**无凭空路径**）；槽位未定义时登记 `TECH-OPEN-*`。
> 📌 槽位「所有权」为 `人工维护` / `AI 生成后接管` 时，第三环重跑**跳过**该槽位（见 `代码工程结构说明.md` §1）。

| 页面 / 组件 | 前端工程 | 目录 / 文件 | 说明 |
|------------|---------|------------|------|
| 页面[N] | {工程名} | {相对路径} | {说明} |

---

## 9. 未决项

| 未决项编号 | 问题描述 | 阻塞点 | 影响范围 | 状态 |
|-----------|---------|--------|---------|------|
| OPEN-FE-{模块}-1 | [...] | [...] | [...] | 待确认 |

---

## 10. 版本记录

| 版本 | 日期 | 变更 | 变更人 |
|------|------|------|--------|
| v1.0 | YYYYMMDD | 初版 | architect-agent |
