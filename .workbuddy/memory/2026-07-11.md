# 2026-07-11 工作记录

## 四层统一模型落地（结构性改造）

用户经多轮共识讨论后拍板并指令「可以开始调整了」，落地**四层统一模型**：同一原子功能（FR-x 为唯一脊柱键）贯穿四种容器视角——01 场景→用户故事 / 02 页面→功能组件 / 03 模块→功能点 / 研发 代码模块→代码功能。场景与模块多对多（经 FR-x），模块跨场景抽取。

### 达成的关键共识
1. 02 的 UI 单元由「功能模块」正名「功能组件」（区别 03「模块」、研发「代码模块」三义）。
2. 01 引入「场景」一级容器，但**不改 §6–§14 编号**（经 §2.4 子节 + §6/§7「所属场景」列落地，避免全仓重编号断裂）。
3. 03 由「FR-x 逐条标注」改为「**模块→功能点两层**」：architect 先按 5 条判据归纳模块脊柱（业务域模块+跨切面模块如鉴权/通知/日志独立，宁细勿粗·待人工校准，模块归纳属需求翻译非架构设计），再逐模块逐功能点展开 9 子项（新增 ⑥异常 + ⑧接口初步规划伪代码，机器可读⑦与伪代码⑧并存）。
4. 02 弹窗二分：复杂弹窗→独立辅助页面[N-A]（挂主页面·记所属场景）；简单弹窗→主页面内联。
5. 04 实体按 03 §1 模块分组，所属模块继承 03 归纳结果(M1/M2…)。
6. 人工校准/合并/重命名后喂编程智能体不在本项目职责内（03/04 产物为初步分解）。

### 本轮落地文件（全部完成）
- **模板**：01（§2.4 场景总览+§6/§7 所属场景列）、02（功能组件+主/辅页面+弹窗二分）、03（整文件重写为模块→功能点两层+9子项+伪代码示例）、04（所属模块继承 03）。
- **prototype 侧 skill**：business-architecture（场景采集+功能组件术语）、page-specs（功能组件+弹窗二分+所属场景回填，修 §13→§14 未决项 bug）。
- **architect 侧 skill**：spec-analysis（加 §2.4 场景抽取+新增 Step1.5 模块初步归纳含5判据+映射草案）、tech-architecture（Step1 模块脊柱含5判据、Step2 模块→功能点9子项、边界补模块≠场景≠代码模块）、data-model（Step1 实体按模块分组·所属模块继承 03）。
- **指令/配置**：CLAUDE.architect.md（你是谁+四层模型+阶段2模块归纳判据+机器可读与伪代码并存+阶段1/3微调+目录树注释）、AGENTS.architect.md（角色+门禁1[场景+模块归纳]+门禁2[模块→功能点9子项]）、opencode.json（architect prompt 全改 + prototype prompt 加场景/功能组件，JSON 校验通过）、agent/architect-agent.md（description+角色+工作方式+约束）、agent/prototype-agent.md（角色+四层统一模型）、CLAUDE.md（阶段1/2 场景+功能组件+弹窗二分+目录树注释）、AGENTS.md（问卷加业务场景块+②功能组件+③弹窗二分+⑥所属场景+门禁1章节号修正§8业务实体/§14未决项）、SETUP.md（03 模板描述）、README.md（architect 描述+多轮沟通+单一权威源+追加当日 changelog）、templates/tech-任务结果摘要.md（完整性提示改模块→功能点）。
- **记忆**：MEMORY.md 因超限被截断→整合去重全量重写（10 节精简版，纳入四层模型为当前权威结构）；本日志新建记录。

全仓校验：无残留「逐条标注」（除历史 changelog）、无错位「功能模块」（除术语澄清语境）、四层模型关键词覆盖 opencode/README/CLAUDE/agent/templates 多文件。

### 日期归属修正（事后复核）
- 本轮四层模型实际发生于 7-11；原误写入 `2026-07-10.md` 并误标 README `2026-07-10（二）`。已纠正：`2026-07-10.md` 还原为仅含 7-10 的 architect 结构重定义收尾；本文件为 7-11 独立日志；README L49/50 交换顺序（architect重定义=7-10 在前、四层模型=7-11 在后）；MEMORY.md §2/§3 日期更正为 2026-07-11。

## 项目业务逻辑全新审计（当日下午）

用户要求对整个项目做全新独立审计，忽略前置结论预设，验证业务逻辑顺畅准确。

- **方法**：实际读取核心指令（CLAUDE/CLAUDE.architect/AGENTS/AGENTS.architect）、四模板（01/02/03/04）、全部 skill（business-architecture/page-specs/spec-analysis/tech-architecture/data-model/delivery/tech-delivery/tech-common-rules/pencil-executor）、opencode.json、技术通用规则模板，做跨文件术语/章节号/流程一致性比对。
- **结论**：工程链路贯通、四层统一模型（FR-x 脊柱）贯穿全链路、术语三件套无混用、章节号体系自洽，整体顺畅准确。
- **发现并直接修复 3 处准确性缺陷**：
  1. 【阻断】`templates/prd-任务结果摘要.md:59` 未决项写「01 §13」→ 应为「01 §14」（同文件 L57 已用 §14，§13 是异常）。
  2. 【重要】`skills/tech-architecture/SKILL.md:56` 错误码分段规则引用「技术通用规则 §8」→ 应为「§5」（§5 统一约定含错误码分段，§8 是部署）。
  3. 【轻微】`CLAUDE.md:191` + `AGENTS.md:248` 决策门禁话术「接口规划」旧术语 → 改为「技术规格（03）/ 数据模型（04）」（05 已并入 03）。
- **遗留观察（非阻断）**：`document/超跃/运动社区_20260708/` 老任务仍用旧格式（功能模块/逐条标注），按「历史产物维持现状不动」约定不整改；README changelog 历史叙述含旧术语为时间线记录，正确。
- **交付物**：`项目审计评估报告_2026-07-11.md`（根目录，含审计范围/核验表/发现明细/验证通过项/遗留观察/总结）。

## architect-agent「生成中遇不确定必须暂停确认」硬规则固化

用户对审计后提出确认诉求：技术智能体在经 01/02 分析直至生成 03/04（含技术工程摘要、技术通用规则）全程，遇不确定内容或复杂逻辑须与用户多轮对话确认、确认后才生成材料，非内部自动生成。核验发现：门禁 0–3 + 阶段1 澄清 + 收口后置已保证「出稿前确认」，但**生成 03/04 过程中**遇不确定/复杂逻辑无「必须暂停反问、确认后落笔」硬规则（原仅允许 TECH-OPEN-* 待确认标记 + 门禁2/3 审阅发现），存在执行口径缝隙。用户拍板「强化指令」，已在 4 处补硬规则：
1. `CLAUDE.architect.md` 核心约束⑨（生成中遇不确定必须暂停确认）
2. `AGENTS.architect.md` 对话风格第 4 条「生成中遇不确定即停」
3. `skills/tech-architecture/SKILL.md` Step 2 内联 ⚠️ 硬规则（写功能点④处理逻辑时）
4. `skills/data-model/SKILL.md` Step 2 内联 ⚠️ 硬规则（写状态机/关联/分表时）
规则与门禁 0–3 互补：门禁保「出稿前确认」，本规则保「生成途中遇不确定即停」。
