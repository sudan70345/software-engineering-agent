# 2026-07-08 工作日志

## 工程整改：prototype-agent 双段式重构

用户调整了 `agent/prototype-agent.md` 的核心思路：整个任务拆为两部分——
- **第一部分 · 需求分析（必做）**：阶段1 业务架构 → 阶段2 逐页面规格，产出工具无关的「需求包」（01-business-architecture.md + 02-page-specs.md）。
- **第二部分 · 原型生成（可选 · 当前绑定 Pencil 方案）**：可插拔解决方案，决策门禁触发后提示用户打开本地 Pencil 桌面应用 + 启动 Pencil MCP 服务。

### 用户拍板的三个决策点
1. 决策门禁放在**需求包确认后**触发（用户看到成果再决定）。
2. 需求文档**完全去 Pencil 化**，Pencil 作为可插拔方案；需求包里只留中性序号 `页面[N]`，Pencil 命名（P{N}_ / P{N}D{M}_）由 pencil-executor 阶段推导，不回写需求包。
3. 目录结构暂不变。

### 已落地修改
- 5 个主文档统一叙事与命名：CLAUDE.md / agent/prototype-agent.md / AGENTS.md / README.md / SETUP.md。
- skills/page-specs/SKILL.md：去 Pencil 化，改中性序号 `页面[N]`、弹窗[1]，画布尺寸等移至 pencil-executor。
- skills/delivery/SKILL.md：新增出口A「需求包交付摘要 requirement-summary.md」（Pencil 跳过时收口）。
- skills/pencil-executor/SKILL.md：新增第50行命名推导说明，明确页面/弹窗命名在本阶段由需求包映射。
- skills/business-architecture/SKILL.md：删除「调用时初始判定」（阶段1前问原型，与门禁冲突），改为指向 CLAUDE.md 单一决策门禁。

### 一致性复核结论
- 关键术语（双段式、需求包、决策门禁、可插拔、Pencil 桌面应用、Pencil MCP 服务）在 5 主文档中用法一致。
- 无「三轮」「Pencil 页面名」等旧术语残留；无「阶段1启动前决定原型」的碎片化矛盾。

---

## 需求包定位澄清 + 三阶段工程链路写入核心约束

用户澄清：01+02 不是直接给 coding agent 编码用的，而是**生成技术架构文件所需的上游产品功能基础说明**。

### 工程链路（写入核心约束）
`产品需求分析文件（需求包）` → `技术架构 agent 生成功能设计文件` → `编码 agent 生成代码`。
本 agent 处于第一环，产出是上游产品功能基础说明，供技术架构 agent 作为输入。

### 已落地修改（5 文件）
- CLAUDE.md：核心原则新增「工程链路（三阶段）」；需求段约束新增第4条「职责边界」（需求包只描述产品功能与业务行为，不设计技术方案——数据模型/API/技术栈/字段级实现契约归技术架构 agent）。
- agent/prototype-agent.md：description 标明「工程链路第一环」；职责说明改写为「上游产品功能基础说明」并点名下游链路。
- AGENTS.md：角色句补「工程链路的上游产品功能基础说明」。
- README.md：关键约束补「工程链路」一条。
- SETUP.md：使用方法补工程链路说明（需求包始终可被下游独立消费，Pencil 仅为可选视觉化辅助）。

### 重要边界纠正
- 我早前把「数据模型/接口契约/字段级类型」（技术设计层）误列为本 agent 缺失项——按新链路，这些属于**技术架构 agent**，本 agent 不应产出。应剔除该方向的修复建议。
- 仍属本 agent 的产品层缺口（在「产品功能基础说明」口径下依然有效）：页面/模块状态(P1)、模块/元素/交互冗余去重(C3)、页面编号统一为「页面[N]」(C1)、全局范围/非目标(M5)、业务规则作为产品行为描述(M2-light)、需求可追溯ID(M4)、未决项管控(M11)、异常/边界(M8)、角色差异(M10)。可在后续按需补。

---

## 两个需求收集 Skill 的严谨复核 + 全量改进落地

基于核心约束「只做产品功能基础说明、不设计技术方案」重新复核 business-architecture 与 page-specs 两个 skill，剔除误判的技术设计项（数据模型/接口/字段类型），保留产品层缺口，给出三梯队改进建议并被用户全量接收。

### 已落地修改（接收全部改进建议）
**skills/business-architecture/SKILL.md（覆盖 B-A~B-G、C1、C2、C3）**
- B-E/C1：页面编号统一为「页面[N]」贯穿全工程（根因在 01 用裸「页面1」）。
- B-F/C2：调用上下文删除「继续新建/更新」，对齐 CLAUDE.md（维护/查阅 vs 维护/修改需求文档）。
- B-A：新增「范围与非目标（In-scope / Non-goals）」段。
- B-B：新增「角色与权限（产品级）」矩阵段。
- B-C：新增「验收口径 / 成功标准」段；并修 Step1 提问（原七维度缺成功标准，与产出错位）→ 扩为十维度（加角色/范围/验收）。
- B-D：新增「全局业务规则（产品行为，非技术实现）」段。
- B-G：放宽功能类型枚举（后台CRUD/交易/电商/社交/工具类等）。
- C3：约定 01「页面流转」为宏观主路径，02「交互定义」为微观细节、以 02 为权威。

**skills/page-specs/SKILL.md（覆盖 P-A~P-L）**
- P-A：每页新增「页面状态」（空/加载/错误/受限）。
- P-J：新增「内容分工约定」明确模块清单/关键元素/交互定义职责，消除三重冗余。
- P-B：新增「业务规则（产品行为）」段。
- P-C：新增「页面间传参」段。
- P-D：模块增加「可见角色」字段。
- P-E：新增「全局异常与边界交互」说明。
- P-H：移除「48×48」像素实现示例，改为产品意图表述；设计要点注明"尺寸写产品意图而非实现像素"。
- P-K：规定 Stage2 新页面必须回写 01。
- P-F：模块引入需求ID（页面[N]-M[k]）。
- P-G：新增「未决项登记」表，禁止静默留白。
- P-I：补全模块类型枚举（tree/tabs/popover/pagination/upload/drawer/stepper 等）。
- P-L：Step4 门禁2 前新增「完整性校验清单」。

**AGENTS.md（一致性对齐，因两 skill 引用其门禁模板）**
- 开场引导功能类型枚举放宽；澄清模板补「角色与权限/范围与非目标/成功标准」三段。
- 确认门禁1 高亮项扩展到 8 项（含范围/角色/验收/业务规则）。
- 阶段2 页面对话模板补全 ⑤状态与异常 ⑥业务规则与传参，模块内容补"不应出现内容/角色差异"。
- 页面确认模板补「页面状态/业务规则」。
- 确认门禁2 主页面清单编号统一「页面[N]」，并加「完整性校验」前置说明。

### 一致性复核结论（最终）
- 当前工程文档集（CLAUDE/AGENTS/agent/README/SETUP + 两个 skill）零残留：无裸「页面1」、无「继续新建」、无「三轮」、无「48×48」像素泄漏。
- "Pencil 页面名" 仅出现在 pencil-executor/delivery（第二阶段 .pen 映射表头，属正确用法）。
- 全工程唯一页面编号约定：「页面[N]」贯穿 01/02/executor/门禁。
- document/ 下历史产物（含裸编号/48×48/Pencil页面名）按用户要求不予处理。

---

## delivery 与 ui-design-elements 对齐调整

用户要求把交付摘要模板与 UI 令牌模板也对齐前述新增字段与引用关系。

### skills/delivery/SKILL.md（出口 A 需求包交付摘要）
- 概览「功能类型」由窄枚举改为「参见 01 文档需求描述」，与放宽后的枚举一致。
- 新增：范围摘要、角色与权限摘要、全局业务规则摘要（产品行为）、未决项表、完整性与交接提示（呼应 P-L 完整性校验与 P-G 未决项）。
- 通知框新增「工程链路定位」与「交接前确认未决项闭合」，呼应三阶段链路。

### templates/ui-design-elements.md（视觉令牌库）
- 顶部新增「引用关系（与需求包联动）」说明：本文件是 page-specs「页面状态」「业务规则」的视觉令牌来源；并注明属视觉层（主要供 Pencil 原型生成），需求包阶段不依赖它。
- 末尾新增「状态-令牌映射参考」节：将产品状态（空/加载/错误/受限/正常）+ 业务反馈（成功/警告/危险/信息）映射到色板令牌，建立与 02-page-specs 的双向引用。
- 强调需求包阶段只引用状态名称、不绑定令牌；令牌在第二部分由 pencil-executor 注入。

---

## delivery 与 pencil-executor 追加对齐（用户「继续修改」）

用户同意上一轮提出的两条追加项，已落地：

### skills/pencil-executor/SKILL.md
- Step 2「准备 UI 设计要素」新增提示：明确 `ui-design-elements.md` 末尾「状态-令牌映射参考」节为状态/反馈令牌的唯一来源，绘制状态变体与业务反馈时不得自行发明颜色。
- Step 5.2「分批绘制」新增「状态与反馈令牌」条目：绘制空/加载/错误/受限等状态与成功/警告/危险/信息反馈时，依据该映射节选取令牌（danger→#FF3B30 等），与 02-page-specs 的「页面状态」「业务规则」语义对齐。

### skills/delivery/SKILL.md
- 需求包交付摘要模板（requirement-summary.md）新增「页面状态覆盖清单」小节：逐页列出状态覆盖（正常/空/加载/错误/受限），注明不适用项，供下游推导各状态界面表现层。

---

## 工程项目层级 + 工程公共规则 优化（用户「现在要对实施过程和结果物进行优化」）

用户要求引入「工程项目」概念：prototype 下按业务归属划分工程，工程内多任务共享一份公共规则文件；初始使用时分析业务相似度决定新建或纳入已有工程；收集过程抽取通用规则入公共文件，所有同工程任务须读取/遵守/维护，Pencil 生成亦遵循。

### 设计决策（用户未反对即采纳）
- 目录结构升级两级：`document/{工程名}/{任务名}_{YYYYMMDD}/`，保留"任务名+日期"旧命名。
- 工程级共享文件命名 `工程公共规则.md`，置于 `document/{工程名}/`。
- 初始判定逻辑单一事实源在 CLAUDE.md「调用与决策·初始使用」；AGENTS.md 仅引用并加「初始使用分析」段落。

### 已落地修改（9 文件 + 1 新增模板）
- CLAUDE.md：核心原则加「工程项目归属」「工程公共规则（跨任务共享）」；任务目录结构改为两级树（含 工程公共规则.md）；新增「初始使用：工程项目归属判定」段（扫描现有工程→相似度分析→有相似纳入/无相似询问新建或纳入已有）；核心约束需求段加"工程公共规则"第5条、原型段加"遵循工程公共规则"；关键文件引用表加 工程公共规则.md 行。
- agent/prototype-agent.md：description 与角色/职责补工程项目组织与公共规则维护。
- AGENTS.md：新增「初始使用分析（工程项目归属判定）」段落；门禁1/2/决策门禁路径升级为 `document/{工程名}/{task_dir}/`，决策门禁补工程与公共规则行。
- README.md：主要输出目录改两级 + 工程公共规则；关键约束加工程项目归属与公共规则跨任务共享。
- SETUP.md：使用方法路径升级、自定义UI路径升级、归档 mv 改 `document/{工程名}/{task_dir}/ document/{工程名}/history/`、项目文件清单树加 `{工程名}/` 层与 工程公共规则.md、templates 加 工程公共规则.md。
- skills/business-architecture/SKILL.md：前置条件改「已完成工程项目归属判定」；调用上下文补读取/初始化 工程公共规则.md；Step1 补已有工程复用提示；Step2 补「工程公共规则抽取（必做）」；01 模板角色与权限节补引用说明。
- skills/page-specs/SKILL.md：输入加 工程公共规则.md（须先读取遵守）；Step1 补读取公共规则复用角色/术语；模块 可见角色 补引用；Step2 后补「通用模块/标准抽取（必做）」。
- skills/pencil-executor/SKILL.md：输入加 工程公共规则.md；Step2/5.2 补「遵循工程公共规则」；路径 replace_all 升级两级；"已指定任务目录"改 `document/{工程名}/{task_dir}`。
- skills/delivery/SKILL.md：路径 replace_all 升级两级；需求包清单加 工程公共规则.md 行、概览加所属工程；两处通知框补工程与公共规则行；归档 mv 改工程内 history。
- templates/工程公共规则.md（新增）：工程公共规则模板（工程基本信息/角色权限/通用业务规则/UI标准/术语表/通用模块/维护记录），供新建工程初始化复制。

### 一致性复核结论
- 旧裸路径 `document/{task_name}` / `{task_name}_{YYYYMMDD}` / `{task_dir}/` 在文档集零残留。
- `工程公共规则.md` 与 `document/{工程名}/{task_dir}` 在 9 文件中引用一致。
- 初始判定逻辑仅 CLAUDE.md 定义，AGENTS/business-architecture 仅引用。
- document/ 下历史扁平任务目录（商品基础管理模块_20260615 等）按用户要求未纳入新模型、未改动。

## 工程公共规则文件改名为英文（用户最新要求）

用户要求 `工程公共规则.md` 改为英文命名。已改名为 `产品通用规则.md`：
- 模板文件 `templates/工程公共规则.md` → `templates/产品通用规则.md`（标题行同步为 `Project Common Rules`）。
- 全工程 9 个文件（CLAUDE/AGENTS/README/agent/SETUP + business-architecture/page-specs/pencil-executor/delivery 四个 skill）中 `工程公共规则.md` 引用全部 replace_all 为 `产品通用规则.md`。
- 保留中文概念称谓（如章节标题「遵循工程公共规则」、约束名「工程公共规则（跨任务共享）」）作为中文文档内的概念标签，仅文件名与文件引用改为英文。
- 复核：全工程已无 `工程公共规则.md` 残留。

## 移除 history 归档 + delivery 重构（用户「有几个要先调整」）

用户两项调整：
1. 不再有 history 目录、不存在手动归档需求（已引入工程级组织）。
2. `skills/delivery/SKILL.md` 重构：基于 01/02 新建或修改生成最新结果摘要，与 Pencil 调用及其结果完全无关；输出按日版本化 `requirement-summary_{YYYYMMDD}.md`（当日累积同日文件、次日生成完整新文件形成版本）。

### 已落地修改
- 移除 history：CLAUDE.md（目录树删 `history/` 两处、删手动 mv 归档段、delivery-summary 改归属 pencil-executor）、AGENTS.md（门禁"否"分支与迭代规则改 requirement-summary_{YYYYMMDD}）、README.md、agent/prototype-agent.md、SETUP.md（删归档 mv 段、文件树删 history 层、delivery 注释）。
- delivery/SKILL.md 重写：定位"基于 01/02 新建或修改生成结果摘要，与 Pencil 无关"；触发条件改为"01/02 变化即触发"；输出 `requirement-summary_{YYYYMMDD}.md` 按日版本化；模板保留范围/角色/业务规则/页面状态覆盖/未决项/完整性/工程链路定位；通知框补工程与历史版本说明。
- pencil-executor/SKILL.md：第71行 `requirement-summary.md` → `requirement-summary_{YYYYMMDD}.md`；新增「第二部分交付」章节，自行产出 `validation-report.md` 与 `delivery-summary.md` 及完成通知（与 delivery 解耦，明确 Pencil 交付由其自有负责）。

### 一致性复核结论
- history / mv prototype 在文档集零残留。
- delivery-summary.md 仅归属 pencil-executor（CLAUDE/README 标注可选、pencil-executor 新增章节生成）；delivery/SKILL.md 已不引用。
- requirement-summary 全工程统一为 `_YYYYMMDD` 版本化命名。
- document/ 下历史产物（含历史 delivery-summary.md）按用户要求未处理。

---

## 三层记录体系落地（替换「按日版本化 requirement-summary」旧设计）

用户经多轮讨论拍板：把「当前态」与「历史」彻底分离。最终规范：
- 工程级 `工程摘要.md`（索引式·轻，单文件原地更新）：每个任务一行（任务名·状态·一句话结论·→链接）。
- 任务结果级 `任务结果摘要.md`（独立文件·原地更新）：状态封面，指向 01/02 与原型，不写进 01/02。
- 任务操作级 `任务操作记录.md`（单文件·只追加）：每条四字段（计划时间/执行内容/执行结果/复核结果），含需求侧+Pencil 侧。

### 已落地修改
- CLAUDE.md：新增「三层记录体系」整节（目录树加 任务结果摘要.md/任务操作记录.md/工程摘要.md 三行；工作流段改「生成任务结果摘要→触发门禁」并写清三文件同步语义；关键文件引用表 delivery 行改述；line90 保留旧版已废弃声明）。
- skills/delivery/SKILL.md：重写为基于 01/02 生成/原地更新 `任务结果摘要.md` + 向 `任务操作记录.md` 追加需求侧条目 + 刷新 `工程摘要.md`；与 Pencil 解耦。
- skills/pencil-executor/SKILL.md：保留「第二部分交付」章，新增「三层记录体系同步」节点（更新任务结果摘要原型态/追加操作记录原型侧/刷新工程摘要），明确 pencil-execution-log.md 为 Pencil 内部明细、任务操作记录.md 为统一流水。
- AGENTS.md / SETUP.md / agent/prototype-agent.md / README.md：4 文件共 7 处残留 `requirement-summary_{YYYYMMDD}.md`（按日版本化）与「需求包结果摘要」旧措辞，全部替换为新三层口径。

### 一致性复核结论
- 全工程零残留「requirement-summary / 按日版本化 / 需求包结果摘要」，仅 CLAUDE.md:90 保留有意「已废弃」声明。
- 三层文件命名（工程摘要.md/任务结果摘要.md/任务操作记录.md）在 9 文件中引用一致。
- document/ 下历史产物未纳入新模型、未改动。
- templates/ 三层文件模板（工程摘要.md / 任务结果摘要.md / 任务操作记录.md）、CLAUDE.md 三层体系段 + 引用表均已落地。

---

## 全工程深度审计 + P1/P2 修复

全工程 9 文件 + 5 模板逐项审计，结论：逻辑闭环完整、无断裂/死路/自相矛盾。

### P1（遗漏）修复
- SETUP.md 项目文件清单补全 3 个新模板文件。
- README.md 输出目录补全工程级 `工程摘要.md` + 记录文件 `任务结果摘要.md` / `任务操作记录.md`。
- CLAUDE.md「已指定任务目录」分支补「维护需求文档后由 delivery 刷新三层文件」步骤。

### P2（优化）修复
- delivery/SKILL.md 的 Step 2/3/4 各加模板引用指令（首次创建从 templates/ 复制、以模板为单一权威源），消除内联模板漂移隐患。
- business-architecture/SKILL.md 新建工程前置条件补 `工程摘要.md` 懒创建说明。

---

## 全工程逻辑一致性审计 + 三批修复（用户「重新读取，从入口审计」）

用户要求清空缓存印象、重读 14 个工程定义文件后独立审计。审计出 11 项问题（🔴2 高 / 🟡4 中 / 🟢5 低），按三批全部修复：

### 批次1·收口权威源（🔴1 决策门禁/工程归属判定单一权威源冲突、🔴2 原型编号四套词汇、🟡3 Pencil 约束三重定义）
- `pencil-executor/SKILL.md`：标题「阶段 3」→「第二部分」；「## 注意：何时调用…（新增）」→「### 触发条件」并修复标题层级。
- `agent/prototype-agent.md`：决策门禁段改为纯引用 CLAUDE；约束段改为指向 pencil-executor（删除 batch_design/十六进制色重述）。
- `CLAUDE.md`：第二部分子步骤去 3a-3d、改为「流程骨架 + 全量定义于 pencil-executor」；原型段约束整段改为「索引 + 指向 pencil-executor」，删除重复清单；两处治理声明由「其他文件仅引用」改为准确表述（CLAUDE 定义逻辑、AGENTS 话术/README/SETUP 概述）。
- `AGENTS.md`/`README.md`/`SETUP.md`：决策门禁处加「判定逻辑详见 CLAUDE.md」pointer。

### 批次2·补缺口与一致性（🟡4 02 模板缺未决项、🟡5 SETUP 树缺工程摘要、🟡6 CLAUDE 低估任务结果摘要、🟡7 3a-3d 与 Step 映射）
- `page-specs/SKILL.md`：02 页面模板补「未决项」字段（每页），与完整性校验、任务结果摘要闭环。
- `SETUP.md`：工程级树补 `工程摘要.md`。
- `CLAUDE.md`：任务结果摘要「内容」描述对齐真实 11 章节模板。

### 批次3·清理术语与痕迹（🟢8 任务名→任务目录、🟢9 角色与权限矩阵→角色与权限、🟢10 痕迹）
- `CLAUDE.md`：工程摘要描述「任务名」→「任务目录」。
- `delivery/SKILL.md` + `templates/任务结果摘要.md`：「角色与权限矩阵」→「角色与权限（产品级）」。

### 复核
- grep 确认旧残留（阶段3/（新增）/3a-3d/角色与权限矩阵/任务名·当前状态/单文件多页面约束（Critical））全清零。
- 长期记忆 MEMORY.md 新增「文档治理与单一权威源」约定节，并修正三层摘要「任务名→任务目录」。

### 未处理项（非逻辑错误，待用户定）
- 🟢11：`document/` 下 3 个历史任务目录（集采商城_V1_20260614 等）仍用旧文件集（requirement-analysis.md / prototype-work-checklist.md / delivery-manifest.md，无 01/02、无三层文件），与现行规范不平齐。属历史产物，用户此前要求不处理；如需可加免责说明或迁移。
