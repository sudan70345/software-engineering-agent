# 项目长期约定（software-engineering-agent）

## 1. 工程链路与双智能体
三阶段链路：`需求分析(prototype-agent) → 技术分析(architect-agent) → 编程智能体(外部)`。
- prototype-agent 产出需求包（01-需求规格说明书.md + 02-UI交互规格说明书.md）= 上游产品功能基础说明。
- architect-agent 产出 tech/（模块级 03 后端 / 04 数据 / 05 前端，见 §14），定位=**需求翻译器/中转站，不做架构设计**（分层/技术选型/部署/非功能属**外部技术架构**，由**工程基线**承载，见 §16）。
- 两智能体同工程共享 `document/{工程名}/` 与 `skills/`，同任务共享 `{任务名}_{YYYYMMDD}/` 锚点；prd/ 与 tech/ 子目录数据独立，architect 只读 prd/、只写 tech/。
- 独立指令文件：prototype=CLAUDE.md/AGENTS.md；architect=CLAUDE.architect.md/AGENTS.architect.md；均在 opencode.json 注册。

## 2. 四层统一模型（2026-07-11，当前权威结构）
同一原子功能（**FR-x 为唯一脊柱键**，贯穿四层）在四种容器视角：
- 01 需求：**场景**（业务情境，一级容器 §2.4）→ 用户故事(FR-x)
- 02 UI：**页面[N]/辅助页面[N-A]** → 功能组件(FR-x)
- 03 技术：**模块**（技术域业务聚合）→ 功能点(FR-x)
- 研发：代码模块 → 代码功能

术语三件套互不同义：02「功能组件」(UI单元) ≠ 03「模块」(业务域容器) ≠ 研发「代码模块」。
场景与模块**多对多**（经 FR-x），模块跨场景抽取。
- 01 引入场景但**不改 §6–§14 编号**（经 §2.4 子节 + §6/§7「所属场景」列落地，避免全仓重编号断裂）。
- 02 弹窗二分：复杂弹窗（创建/修改/配置类，独立表单+校验+提交）→独立辅助页面[N-A]（挂主页面、记所属场景）；简单弹窗（提醒/反馈/确认）→主页面内联一句话。

## 3. 03 后端 architect 结构（2026-07-11；05 前端见 §14）
- 03 = **模块→功能点两层**：§1 模块划分总览（模块归纳判据 + 模块总览表 模块/类型/覆盖能力/含FR-x/待校准）；§2 逐模块（模块定义/边界段：核心业务对象/统一业务逻辑/聚合FR-x/关联场景/关联模块/边界 + 功能点块）；§3 错误码；§4 技术风险。
- **模块归纳判据(5条)**：①主对象归属 ②同对象聚合为业务域模块(覆盖完整CRUD) ③跨切面能力独立成模块(鉴权/通知/日志，被多模块共享且独立演进，**不并入业务模块**) ④跨对象功能按核心能力归属、调他模块记「关联模块」 ⑤粒度宁细勿粗。模块归纳属需求翻译非架构设计；architect 拿不到完整工程结构，产物为**初步分解(偏细·待人工校准)**，人工校准不在本项目职责内。
- **功能点展开（现行）**：03 §2 **接口内联**（后端业务上下文 + 读接口「查询语义」）；接口**机器可读定义唯一在 `tech/接口契约.md`**（端点/schema/状态码/错误码/operationId 总表）。**「9 子项 / ⑦⑧」为已废弃旧编号，勿再引用**（见 §13、§14）。
- 04：实体按 03 §1 模块分组，「所属模块」继承 03 归纳结果(M1/M2…)；从 03 §2 功能点输入/输出/关联数据派生。六维确认(表/字段/建表DDL/索引DDL/关联/状态机)。
- 阶段 0+3 阶段 + 4 门禁：0=技术通用规则确认 / 1=需求解析(含模块初步归纳草案) / 2=技术规格(模块脊柱+功能点9子项) / 3=业务表结构。
- 历史：05-接口设计、06-模块详细设计模板已删（并入03）；skills/api-design、skills/module-design 已删。

## 4. 双段式工作流（prototype）
- 第一部分·需求分析(必做)：阶段1 业务架构(同时创建 01+02 基线，含 §2.4 场景 + 主/辅页面总览) → 阶段2 逐页双向落档(同轮向 02 §4 写页面规格[功能组件/主辅类型/所属场景]、向 01 §6/§7 追加功能条目[标所属场景]+§9-§13)。两份经「页面[N]」对齐。
- 决策门禁(需求包定稿后三选一)：①原型设计(Pencil) ②技术分析智能体 ③结束。
- 第二部分·原型生成(可选)：调 Pencil MCP 前提示用户开 Pencil 桌面应用+启动 MCP。

## 5. 01/02 内容约束
- 需求包**工具无关**：禁写 Pencil/绘图实现细节；页面用中性「页面[N]」，Pencil 命名 P{N}_/P{N}D{M}_ 由 pencil-executor 推导不回写。
- 需求包**严禁技术实现内容**(接口/数据模型/技术选型)，归 architect；访谈中用户提及记为「用户技术倾向/约束」交接时移交。
- 01 §8 业务实体=业务概念目录(领域词汇)，仅列概念与业务属性，禁写字段类型/表名/索引。
- 01 五大模块结构化：§6 用户故事(验收多条·覆盖成功/失败/边界态)、§9 业务规则(触发/判定/失败输出/降级，禁写事务回滚/幂等键/锁)、§10 输入(增默认值列·嵌套用each_item)、§11 输出(成功/失败分述·失败仅记提示语义)、§13 异常(强制穷举·发生时机/系统处置/用户体验语义/恢复策略)。弹性：复杂展开、简单精简。
- 失败提示边界：语义/映射规则归 01(供 architect 做错误码映射)，视觉呈现(Toast/弹窗/位置)归 02。
- 通用术语澄清：金额/时效/统计/易混淆专有名词须与用户确认含义与计算规则，写入工程 `产品通用规则.md` §4 通用术语表。
- 未决项分置：01 §14(OPEN-x 功能侧)、02 §6(OPEN-UI-x)；与任务结果摘要闭环。FR-x/BR-x 编号。

## 6. 三层记录体系
「当前态/历史」分离，互不污染，产品侧(prd-)与技术侧(tech-)各独立：
- 工程级 `产品工程摘要.md`(原地更新)：§0 工程概览(工程名/业务领域/一句话定位=工程基本信息只在此) / §1 任务清单(目录·状态·结论·链接) / §2 工程维护记录。
- 任务级 `prd-任务结果摘要.md`(独立·原地更新)：状态封面，指向 01/02 与原型。
- 任务级 `prd-任务操作记录.md`(单文件·只追加)：四字段 计划时间/执行内容/执行结果/复核结果。
- 刷新时机：01/02 或原型变动→刷结果摘要+工程摘要；每次操作后→追加操作记录。
- 小版本号规则：每次 01/02 更新/优化，`文档版本`小版本+1(v1.0→v1.1，结构性重构才大版本+1)；architect 因上游更新而更新时 tech 文档(03/04)小版本同步+1。

## 7. 进入执行流程(CLAUDE.md「调用与决策」Step 0–5 为唯一权威)
- Step 0 工程研判(必做)：读候选工程 `产品工程摘要.md` 作研判依据(`产品通用规则.md` 阶段1读)。
- Step 1 归属+任务类型判定(新建/维护，维护读原 01/02 作基线)。
- Step 2 跨任务一致性研判(必做)：识别 定义偏差/描述偏差/场景重复/前后不一致，发现即提醒用户选「放弃」或「更新」。
- Step 3 执行；Step 4 决策门禁三选一+小版本号；Step 5 技术侧联动(维护且 tech/ 已存在→提醒调 architect 对齐，其 tech 小版本同步+1)。

## 8. 工程级两份文件职责严格分离
- `产品工程摘要.md`=工程全貌(§0 概览[工程基本信息只此]/§1 任务清单/§2 维护记录)。
- `产品通用规则.md`=纯标准(§1 角色权限/§2 通用业务规则/§3 通用UI标准/§4 通用术语表/§5 通用模块清单/§6 规则文件变更记录)，**禁写工程基本信息**。
- 架构侧镜像：`技术工程摘要.md`(§0 概览+技术栈/部署 /§1 技术任务清单 /§2 维护记录)；`技术通用规则.md`(§1 并发容量/§2 语言/§3 框架/§4 规范/§5 约定/§6 领域模型/§7 分层/§8 部署/§9 非功能/§10 变更记录)。
- business-architecture 抽取通用内容只抽标准类，工程基本信息不入通用规则。

## 9. 命名与单一权威源
- 模板/生成文件统一中文，同名文件加 `prd-`/`tech-` 前缀(`交接索引.md` 不改名)。
- 技术模板带序号+说明书：仅保留 `03-技术规格说明书.md`、`04-数据模型说明书.md`(模板名=生成文件名)。
- skill 不含模板：模板内容只放 `templates/`，skill 仅引用(首建从模板复制填占位符)，避免漂移。
- `templates/01-需求规格说明书.md`、`templates/02-UI交互规格说明书.md`、`templates/03-*`、`templates/04-*` 为骨架唯一权威源。
- 决策门禁/工程归属判定逻辑唯一在 CLAUDE.md；原型端 Critical 约束权威源=`skills/pencil-executor/SKILL.md`；其余文件仅引用不另立。
- `skills/<dir>/` 目录名(如 tech-architecture)为加载标识不动。

## 10. 工作约定
- **流程类/重定义改动须先给方案对齐、经确认再落地**，不得跳过确认直接改文件；方案须覆盖权威落点文件+步骤+超出用户原话的自行解释(显式标注)。可用 AskUserQuestion 逐项确认补充解释点。
- `document/` 下历史产物(旧格式任务目录)属用户自有内容，维持现状不动、不迁移、不纳入整改对象。

## 11. 通用规则分层（2026-09-07 权威澄清，覆盖 §1/§3/§8 旧表述）
- **产品侧两层**：项目级=`项目级产品摘要.md`(全貌索引 + §0.1 通用术语表) + `通用校验规则.md` + `通用异常处理.md`(跨系统统一校验/异常要求)；系统级=`系统要求规范.md`(系统定义：是什么/角色/模块/范围，**模板名=产物名**，已废弃旧产物名「00-系统概览.md」) + `modules/*/01、02`。**已废弃** `产品通用规则.md`(旧 §1-§6 单文件) 与 `prd/common/`(旧通用规则目录)。
- **旧「产品通用规则」内容新落点（2026-09-07 方向 A 定论）**：角色/权限→`00-系统概览.md` §3；UI 标准→`系统交互规范.md`+`界面设计要素.md`(视觉)；业务规则(通用)→`通用校验规则.md` §3 + 模块级 01 §9；异常/UI状态/操作反馈→`通用异常处理.md`；字段/格式校验→`通用校验规则.md`；术语表→`项目级产品摘要.md` §0.1。
- **技术侧两层**：项目级=`项目级技术摘要.md`=**纯索引/摘要·不含任何规则/标准/规范(显式冻结)**；系统级=`系统级技术标准.md`(业务约束与跨模块统一约定 + 技术基线引用)+`系统级技术规范.md`(技术目标与业务侧要求)+`数据表设计约定.md`(系统默认字段/软删除过滤)+`modules/*/03、04、05`。**已废弃**项目级「技术通用规则」；**技术架构(语言/框架/分层/部署/非功能)不设文档**，由工程仓库配置文件（工程基线）承载（见 §16）。
- **不对称理由**：产品体验(校验/异常)跨系统一致→项目级；技术栈(前端/后端/移动端)分系统异构→系统级。
- **document/ 历史产物不参与校验**：旧逻辑生成、与当前架构偏差大，不作交叉验证依据；架构自洽性只以「权威源四件套(主指令↔AGENTS↔skills↔templates↔opencode.json)引用自洽」为准。
- **通用标准抽取**=需求梳理时动态发现疑似通用标准→询问用户确认后落点，不预写死判据。

## 12. UI 三层职责边界（2026-09-07 定型，保留拆分不合并）
UI 内容按三层职责划分，视觉值上收唯一权威，禁止跨层重复硬编码：
- **视觉令牌层**=`界面设计要素.md`（色板/字体层级/间距体系/圆角/组件尺寸/阴影），**视觉值唯一权威源**。
- **组件行为标准层**=`系统交互规范.md`（系统级，按钮/弹窗/分页/表格/表单/导航的**交互语义与规则**），只写语义名（如「主按钮」「标题字」），不写具体色值或 px，视觉值引用界面设计要素。
- **页面实例层**=`02-模块-UI交互规格.md`（本页组件清单/布局/交互/状态变体/流转/本页弹窗），引用上两层，只写本页差异。
- 组件交互规则（按钮几态、弹窗遮罩/ESC）=模板 vs 实例关系，属合理分层非重复；弹窗通用行为归系统交互规范 §2，通用异常处理不重复确认弹窗。
- 相对路径：模块文档→项目级(通用异常/校验)=`../../../../`(4级)；模块文档→系统级(系统交互规范)=`../../`(2级)；delivery(基准prd/)→系统交互规范=同级；spec-analysis/tech-architecture(基准tech/)→项目级`../../`、prd`../prd/`。

## 13. 接口内联（2026-09-07 修正，覆盖 §3「9 子项」旧表述）
03 功能点展开已由「9 子项」统一为「**接口内联**」：03 §2 对外接口清单，每接口内联 功能描述/输入 schema/校验/处理逻辑伪代码/输出 schema/异常错误码/关联数据→04；机器可读(schema+状态码+错误码)与伪代码(可译性)并存，不写框架/ORM。全仓已清除「9 子项/⑦⑧」旧编号表述（tech-architecture、agent/architect-agent、03 模板、self-check、opencode.json 对齐）。

## 14. 模块级三件套 03/04/05 + 02 必读 + 端范围（2026-09-15 定型，覆盖 §1/§3/§9 相应表述）
- **architect 模块级产出=三件套（平级）**：`03-{模块}-技术规格.md`（**后端**：模块→功能点两层 + 接口内联）/ `04-{模块}-数据模型.md`（**数据**：表/索引/约束）/ `05-{模块}-前端技术规格.md`（**前端·含移动端**）。**03 与 05 同轮产出**（共享同批 FR-x 与 operationId），门禁仍 4 道、不新增阶段。
- **05 八节**：§0 前端约束引用 / §1 页面与路由 / §2 组件树（02 平铺→树+复用）/ §3 前端状态定义（02 无，新增；作用域 页面·全局·服务端缓存）/ §4 接口消费映射（**只引用 operationId**）/ §5 前端校验（**只取 01 §7「前端·两者」项**）/ §6 页面状态逻辑（02 写「长什么样」、05 写「何时进入·如何恢复」）/ §7 平台差异（依 §6.3 端范围，单端填「不适用」）/ §8 前端落点。模板=`templates/architect-agent/module-level/05-模块-前端技术规格.md`。
- **05 边界（与 03「不写框架/ORM」对称）**：不写框架选型/路由注册方式/请求层封装/状态库/构建配置——均属**前端工程结构常量**（配置文件给定），只写「有哪些页面、什么路由、什么权限、组件怎么组成、需要哪些状态、调哪些接口、何时调、校验什么、异常怎么呈现、落在哪个目录」。
- **architect 输入=01 与 02 同等必读**：01=逻辑与规则主源（FR-x/BR/输入输出/校验分工/异常）→ 03 与 05 §5；02=交互与展示主源（页面[N]/功能组件/交互/状态/流转）→ **05 唯一来源**。**禁止凭 01 推测页面结构**。
- **接口契约中立（2026-09-15 下午修正，覆盖原「唯一在 03 §2」）**：契约定义唯一在 **`tech/接口契约.md`**（§0–§6），前后端与**测试**三方共享边界物；03 §2 承载后端业务上下文、05 §4 只声明消费方式，两者均只引用 operationId。消费方：后端→契约+03+04；前端/移动端→契约+05；测试→契约机械派生。
- **端范围前移至需求侧阶段 1**：`prd/系统要求规范.md` **§6.3 端范围**（Web / 移动端 / 双端并存 + 端形态 + 不覆盖端替代路径）。属**需求范围决策**，技术侧只翻译不发明；多数系统单端→05 §7 多数填「不适用」、不设端分支。端范围缺失时 architect **必须暂停反问**（登记 TECH-OPEN-*），不得自行假定。
- **判据来源（用户原话）**：「代码工程中的配置文件约束了代码工程的结构，需要灌装的是逻辑」→ 结构=常量（归工程）、逻辑=变量（归 AI）；「技术架构是外部的工作定义的，本项目不应负责」→ 换成别的技术栈会改变结论的归外部（architect 只引用），换任何技术栈结论都不变的归 architect（业务翻译）。
- **原未落项处置（2026-09-15 全部闭合）**：① 前端侧校验 → 已落 self-check 检查 4（4a–4d）；② 4 处定位漂移 → 已完成（见 §16）；③ 03 §2 读接口「查询语义」→ 已落 03 模板 §2 并与 04 §3 一一对齐；④ `agent/*.md` 未注册第三套子指令 → 已加停用横幅，**清理仍待办**。
- **工具教训**：批量 Edit 会出现「返回成功但未落盘」，批量改完必须用脚本逐条 needle 校验。

## 15. self-check 五类检查（2026-09-15 定型；检查 5 于傍晚并入）
- self-check 由「三类检查」扩为「**五类**」：检查 1 FR-x 追溯矩阵（含契约侧核对）/ 检查 2 场景覆盖 / 检查 3 章节号·引用·术语一致性（基准含 `接口契约` §0–§6）/ **检查 4 前端侧一致性（05 ↔ 01/02/03）** / **检查 5 业务语义与技术边界一致性**（5a 查询语义齐备 / 5b 与 04 §3 对齐〔仅门禁 3〕/ 5c 全文档技术越界）。检查 4 适用于**门禁 2/3**（门禁 1 无 05，自动跳过）。
- **检查 4 四项**：4a 页面覆盖（02 §1 页面[N]/[N-A] ↔ 05 §1 双向 + 05 §8 落点收口，编号不得重编、不得凭空新增）；4b 接口引用闭合（05 §4 operationId → 03 §2 无悬空 + 错误码 ∈ 03 §2 异常表 + **禁止 05 二次定义 schema/状态码/错误码**；契约独立成文后权威源切换为契约文件）；4c 校验分工（01 §7「前端·两者」↔ 05 §5 双向 + 后端项不得入 05 + 提示语义一致）；4d 边界越界（05 不得写框架/组件库/状态库/请求库选型、路由注册方式、请求层封装、构建配置）。
- **检查 1 纳入 05 时的不对称（关键）**：05 §1/§2「对应 FR」须反向可回溯 01 §2（无幽灵 FR-x），但**不做正向全覆盖**——纯后端 FR-x（定时任务/批处理/数据同步/回调）合法地无前端落点。**前端正向覆盖的锚点是「02 的页面」，不是「01 的 FR」**（即检查 4a）。
- **`operationId` 是跨文档唯一锚点**：`tech/接口契约.md` §2 为**全局唯一登记处**（命名 `{动词}{业务对象}`）；03 §2 每接口引用之、05 §4 只引用不定义。**契约独立成文后权威源=契约文件，不再是 03 §2**；形态切换（Markdown↔yaml）也不改引用方写法。
- **模块级相对路径基准（唯一权威，检查 3 强制）**：`tech/modules/{模块}/` 下 → tech 系统级文件 2 级（`../../系统级技术标准.md` / `系统级技术规范.md` / `数据表设计约定.md` / `模块间接口依赖清单.md` / `接口契约.md`）；→ `{系统}/prd/...` 3 级（`../../../prd/`）；→ 项目级 4 级；同级 03↔04↔05 不加前缀。**1 级系统级引用与 2 级 prd 引用均为死链**（2026-09-15 已在 03/05 模板修正 8 处；document/ 下实例产物沿用旧死链，按约定不纳入整改）。
- **校验脚本注意**：`scripts/consistency-check.sh` 4a「矛盾章节引用」规则基于 01/02 编号（未决项=§10），**05 的未决项在 §9**，故 4a 以 `:>   - ` 排除 self-check 章节号基准登记行（该类行是编号登记非交叉引用，仍受 4b 白名单覆盖）。
- **工具纪律（重要）**：本环境 Edit 存在「返回成功但内容未落盘」现象（2026-09-15 两轮各约 5 处）。**大批量改动后必须用带 count 断言的脚本逐条 needle 复核**，失败则用 Python 直改兜底。

## 16. 边界判据「换技术栈会否改变结论」= 全文档通用（2026-09-15 傍晚定型；4 处定位漂移对齐）
- **判据（唯一·全文档适用）**：**换成别的技术栈，结论会不会变？** 会变 → 外部技术架构 / 技术策略 / 技术配置 → 工程基线承载，本项目**不产出**；不变 → 业务语义 / 跨模块统一约定 → **必须写**。（v2.3 原仅用于「数据访问」，本轮上升为全技术文档通用边界。）
- **① `系统级技术标准.md` 收窄重定位**（新结构：§1 并发容量〔业务〕/ §2 命名与规范约定 / §3 跨模块统一约定 / §4 通用领域模型 / **§5 技术基线引用** / §6 变更记录）：移出 §2 语言·§3 框架·§7 分层·§8 部署·§9 非功能（→工程基线）；§1 只留**业务侧**（日活·量级·峰值 QPS·一致性·延迟·可用性），容量规划手段移出；原 §4/§5 保留**约定类**（数据库命名 / 状态码 / 分页 / 鉴权 / 软删 / 审计 / 多租户），移出代码·提交规范与幂等实现。
- **② `系统级技术规范.md` §6–§10 收窄**（**编号不变，避免引用链断裂**）：§6 核心业务流程（去事务锁）/ §7 性能目标 / §8 幂等业务要求 / §9 缓存业务需求 / §10 事务边界（业务视角）。
- **③ `数据表设计约定.md` 收窄**为 §1 系统默认字段 + §2 软删除过滤（字符集 / 引擎 / 字段类型标准 / 索引命名 / 并发锁 → 工程基线）。**注**：索引命名规范属「研发编码规范」，经用户确认删除。
- **④ 模块 04 §4** 由「并发场景与锁策略」改为「**并发业务场景**」（冲突 / 业务后果 / 期望业务约束）。
- **⑤ 阶段 0 重定义**：从「设计技术标准」→「**确认业务约束与约定 + 登记技术基线引用**」；问卷五类 A 并发容量 / B 命名与规范 / C 跨模块统一约定 / D 通用领域模型 / E 技术基线引用；基线缺失登记 `TECH-OPEN-*`，**不臆造选型**。
- **⑥ self-check 检查 5** 更名「**业务语义与技术边界一致性**」，**5c 由数据访问越界扩为全文档技术越界**（扫系统级三件套 + 03/04/05）；章节号基准同步（系统级技术标准 §1–§6、数据表设计约定 §1–§3）。
- **落地范围（16 文件）**：CLAUDE.architect.md（v2.4）/ AGENTS.architect.md / opencode.json / skills{tech-common-rules, tech-architecture, data-model, self-check, tech-delivery} / 4 个模板 / 交接索引 / tech-系统结果摘要 / 03 模板 / consistency-check.sh（白名单 系统级技术标准≤6、数据表设计约定≤3）/ README / SETUP / docs·最佳实践 / CLAUDE.md（prototype 侧目录树）。终检 `consistency-check.sh` 5/5 通过、全仓无残留旧表述（排除 document/ 与 archive/）。

## 17. 契约单源形态：openapi.yaml 保持后置（2026-09-15 决策）
- **结论**：`openapi.yaml` **未定义、未配置、未启用**——模板库无该文件（全仓 `*.yaml` 0 命中）、无生成器脚本、无校验步骤。当前单源形态=**契约优先（Markdown 单源）**；`03`/`05`/self-check/测试一律以 `operationId` 为锚点，**不产 yaml 不影响链路运行**。
- **决策（用户拍板）**：① 处理档位=**只修悬空检查项**（不做规格补全、不建生成器）；② 将来启用时**落点 = `tech/openapi.yaml`，系统级单文件**，`tags` = 模块名。
- **本轮落地（4 文件 / 5 处）**：`交接索引.md` 第 61 行悬空检查项 →「**可选 · 待启用** + 启用条件 + 落点 + 引用方写法不变」；`tech-系统结果摘要.md` 单源形态行；`CLAUDE.architect.md` 第 297 行；`接口契约.md` §0.1 补落点 + §5 标注可选。终检 `consistency-check.sh` 5/5 通过。
- **启用前需补（备查）**：OpenAPI 版本 / `info`·`servers`·`tags`·`securitySchemes` 映射 / `components.schemas` 命名规则 / 约束表示法（`maxLength`·`pattern`·`enum`·`required`）/ `ApiEnvelope<T>` 与分页的 `allOf` vs 展开 / `x-error-codes` 结构 / 生成时机。

## 18. 02 → 前端布局转译链（2026-09-15 定型；C7③ 闭环）
- **判据落地**：02 只写**布局语义**，05 负责**转译**为布局落点，两侧不得互换；**02 禁写画布尺寸 · 坐标 · px**。
- **四级链**：02（布局语义）→ `系统交互规范`（组件交互语义，已是「语义+令牌、零 px」范式）→ `界面设计要素`（视觉值 / 断点 / 尺寸档位令牌，唯一权威源）→ 05（布局落点）。
- **02 模板 7 处去几何**：§2「画布尺寸」→ **「视口与端形态」**（端形态引用 `系统要求规范.md` §6.3；基准视口区间档位；可滚动）；§3.1 框架图去 px；§3.2 区域表「尺寸」→「尺寸语义」（固定档位 / 自适应）+ **删「坐标提示」**；§4.1「尺寸」→「尺寸档位」；§4.2 位置/尺寸去 px；§8.4 去与 §2 重复的最小宽度、改断点适配；§9.1「画布尺寸」→「弹窗尺寸档位」。
- **令牌补齐**（`界面设计要素.md`）：新增「断点」`sm 768 / md 1024 / lg 1440`；`dimensions` 增 `side_nav_width 200` / `side_nav_width_collapsed 64` / `modal_width_small 400` / `medium 600` / `large 800`；新增「布局与尺寸档位（语义 → 令牌）」词表。**顶栏统一用 `navigation_bar_height`（02 原写的 60px 废弃）**。
- **转译规则唯一口径 = `05` §0「布局转译规则（与 02 的分工）」**：映射表（02 侧语义 → 05 落点 → 取值出处）+ 区域覆盖要求。**改口径说明**：原拟落 `交接索引.md`，因该文件**收口才生成**会造成悬空引用，改落 05 §0（阶段 2 即存在）。
- **校验新增**：`self-check` 检查 4 增 **4e 布局转译**（区域覆盖 / 无凭空新增 / 无绝对几何 / 端与断点对齐）；`consistency-check.sh` 增**检查 6**「布局语义无绝对几何」（扫 02 与 系统交互规范，**排除含「不写…」的禁用行**）；`交接索引.md` 增登记「界面设计要素（视觉值唯一权威源）`prd/界面设计要素.md`」+ 1 条交接检查。
- **skill 同步**：`page-specs`（1g/1h 去几何 + 弹窗档位 + **删除内嵌模板几何片段改引用模板** + 校验清单 2 项）；`pencil-executor` §6.2 弹窗尺寸改由 `modal_width_*` 令牌推导（**02 已不再提供画布尺寸**）；`CLAUDE.md`（prototype 侧布局描述）。
- **终检**：`consistency-check.sh` **6/6 通过**；全仓几何词汇残留 0（仅剩「不写…」禁用表述与 Pencil 绘制层词汇）。

## 19. 逻辑单元建模 · 代码工程结构 · 受控伪代码（2026-09-15 定型；A1/A3/B1/B3 一次贯通，覆盖 §3「逻辑寄生」旧状）
- **逻辑一等对象（LU-x）**：03 §3 由「关键业务流程」升级为「**逻辑单元清单**」（**编号不变**，避免破引用链）；ID 规则与 FR-x 同构（模块内 `LU-1` 顺序编号，全局引用写「{模块名}+LU-x」）。§3.1 总表 `LU-x | 逻辑单元名 | 类型 | 对应 FR | 被引用 | 落点 | 测试锚点`；§3.2 逐单元详述 目的/输入/输出/前置/**逻辑体**/状态迁移表/引用/被引用/落点/测试锚点。
- **抽取规则**：被 **≥2 处引用** / **状态机·算法·跨实体判定** / **统一业务逻辑** → **必须抽**；单接口一次性简单步骤 → 内联 §2。
- **03 §2 语义收口**：标题由「处理逻辑伪代码」改为「**接口编排（调用哪些逻辑单元、什么顺序）**」——§2 只编排契约与业务上下文，**不再承载逻辑体**；非平凡逻辑必须抽去 §3 并此处引用 LU-x。
- **三条硬约束**：① 引用闭合 ② **无孤儿**（无未被引用 LU-x、无悬空 LU 引用）③ 命名归一。
- **逻辑体语法子集（受控伪代码）**：**唯一权威 = `skills/tech-architecture`「逻辑体语法子集」节**。白名单 赋值 / 条件 / 多分支 / 循环（**仅 FOR EACH**）/ 调用（**按 LU-x 引用**，不写方法名）/ 状态迁移 / 抛异常 / 返回 / 原子要求（事务·并发只写「要求」）；强制 入参出参 + 前置 + 错误分支；禁止 框架 API / ORM / SQL / 自然语言长段 / 未定义标识符。**定位 = 可人工审阅 + 可机器解析的最小公共子集**（既非编程语言也非自然语言）。
- **分层表达法（B1 拍板）**：03 逻辑体=受控伪代码 / 01 验收=Given-When-Then / 数据=DDL。
- **`代码工程结构说明.md`（A3；新增于 `templates/architect-agent/system-level/`）**：定位=**外部输入 · 只读**（工程侧提供，architect **不产出**，头部带横幅）。§0 元信息 / §1 工程清单（**可运行性元数据**：构建命令 / 测试命令 / 测试框架 / 启动顺序 / 端口）/ §2 目录骨架 / §3 **槽位定义（含所有权）** / §4 可运行性环境 / §5 版本记录。落「系统级」而非工程级：由工程侧提供且被**所有模块**共享（同 `数据表设计约定.md` 定位）。
- **落点硬约束**：03 §3 每个 LU-x「落点」与 05 §8 前端落点 **必须落在 A3 §3 声明的槽位内**；A3 缺失 → 登记 `TECH-OPEN-*`，**绝不臆造目录**（与「工程结构=常量归工程」判据一致）。
- **槽位级人工保护区（B3）**：所有权三分 —— **AI 生成 / 人工维护 / AI 生成后接管**。第三环重跑**仅重写「AI 生成」**；「人工维护」跳过；「AI 生成后接管」首次生成后跳过后继重跑。

## 20. 自测 exit criteria（2026-09-15 定型；B4）+ self-check 七类
- **落点** = `templates/architect-agent/system-level/系统级技术规范.md` **§15 自测通过判据**；版本记录顺延 **§16**；`consistency-check.sh` 白名单 系统级技术规范 `max=15` → **`max=16`**。
- **四级**：**L1 契约测试（阻断）** / **L2 主链路 E2E（阻断）** / **L3 覆盖率（告警不阻断）** / **例外清单（登记不阻断）**。分界原则：契约测试=单接口形状与错误码；E2E=跨接口主链路。**不可自动验证项**单列：移动端真机 / 三方回调 / 视觉呈现（物理边界，不作为阻断项）。
- **self-check 五类 → 七类**（覆盖 §15 旧表述）：增 **检查 6 逻辑自洽性**（6a 引用闭合 / 6b 文档内交叉引用 / 6c 状态机闭合 / 6d 规则互斥与条件可达 / 6e 命名归一 / 6f 逻辑↔数据字段级〔**仅门禁 3**〕）与 **检查 7 逻辑↔代码结构**（7a 落点完备 / 7b 落点合法 ∈ 槽位 / 7c 反向无空槽 / 7d 所有权核对）；**检查 1 增跨模块契约核对**。检查 6 的 6a–6e 在**门禁 2/3** 执行。
- **两条豁免口径（关键，防误判）**：① 检查 7 依赖 A3 文件，**缺失时不判失败**，改判「登记 TECH-OPEN-*（工程结构未提供）」；② **不得宣称「七类均可脚本化」**——6c/6d 能否机器校验**取决于逻辑体表达法**（受控伪代码 / 状态迁移表可脚本化；退化为自由自然语言则只能人工）。

## 21. 「代码工程结构说明」本体论纠正：本项目推导产出，非外部输入（2026-09-15 定型；覆盖 §19 的 A3 段落）
- **定位（纠偏，唯一权威）**：`代码工程结构说明.md` = **本项目产出**（architect-agent 于**阶段 1** 推导产出），性质「**结构基线快照**」。**不是外部输入**——**从写作角度不存在独立的外部供给方**；早期「外部输入 · 只读 / 由工程侧提供」的定位属**推断错误**（日志曾标「用户原话隐含未显式化」，即非用户要求），已废弃。
- **废弃理由（写进文件头，防回退）**：若无供给方，文件在真实项目中**永不出现**，而 `self-check` 检查 7 依赖它 → 每次退化为「假设缺失 → 登记 `TECH-OPEN-*` → 永远挂着」，等于绕一圈无所得。
- **生成方式 = 推导优先 + 差额问询**：**零提问推导**（端范围 `系统要求规范.md` §6.3 → 需要哪些工程；技术基线 `系统级技术标准.md` §5 + **已有脚手架 skill 的 `assets/scaffold/`**（如 `uni-app-scaffold`）→ 标准目录骨架；模块草案 → 槽位数量与命名）；**差额问询** 只问推不出的（既有代码位置 / 自定义分层 / 人工保护区），**能推的不问**。
- **推导前提（关键洞察）**：**技术栈一旦确定，标准目录骨架即已知常量**——§16「换栈会变」判据的**反向用法**（结构跟着技术栈走 ⇒ 技术栈已知则结构可推导）。故 A3 内容以**推导为主、问询为辅**，不是一份要人填写的问卷。**推导映射不新建文件**，取自各脚手架 skill 的 `assets/scaffold/` 与框架惯例。
- **内容（精简后 = 只留 1 张表）**：**§1 槽位定义**（`槽位ID | 工程 | 路径 | 类型 | 所有权`）。**已删** §0 元信息（版本走 `tech-系统操作记录.md`）/ §2 目录骨架（**「路径」列并集即骨架**，两处描述同一事实=漂移源）/ §4 可运行性环境 / §5 版本记录；**已移出** 可运行性元数据 → `系统级技术规范.md` §15「可运行性元数据（执行前置·登记项）」表（工程/构建命令/测试命令/测试框架/启动顺序/端口）。已去「允许承接的 architect 产物」列。**槽位定义章节号 §3 → §1**（全仓引用同步）。
- **产出时机**：**阶段 1 一次落全**（不拆阶段）；阶段 0 只登记 §5 技术基线引用，不产出该文件。
- **`self-check` 检查 7 口径改**：前置依赖由「**文件是否存在**」改为「**§1 槽位表逐项是否推出**」；缺失口径由「整文件不判失败」改为「**单项不判失败**」→ 登记 `TECH-OPEN-*（该槽位未推导出）`。
- **`consistency-check.sh`**：白名单增 `代码工程结构说明) max=1 ;;`（该文件只有 §1）；提取正则与注释同步。
- **历史表述保留**：`README.md` / `SETUP.md` / `CLAUDE.architect.md` 的**旧修改日志行保留原文**（记 v2.5 当时状态），紧邻新增「夜·追加」/ v2.6 行明确纠偏——**历史与纠偏并存即正确记法**，不做历史改写。

## 22. 跨工具智能体注册入口 + `agent/` 第三套子指令清理（2026-09-15 定型）
- **`agent/*.md` 已删除**（`agent/prototype-agent.md` / `agent/architect-agent.md`）。删除依据：全仓 **0 处活动引用**——`opencode.json` 的智能体注册是**内联 prompt 字符串**（正文指向 `CLAUDE.md`/`AGENTS.md`，**不读** `agent/*.md`），`.opencode/agent/` 约定目录不存在。git 已跟踪，`git checkout HEAD -- agent/` 可恢复。`archive/2026-09-04_*审计评估报告.md` 的「删除或归档 agent/*.md 旧指令」待办由此**闭合**。
- **各 AI 工具的智能体注册入口（权威对照，本工程适用）**：
  | 工具 | 自动加载的**指令文件** | 自动发现的**智能体** |
  |---|---|---|
  | opencode | 无（靠 `opencode.json` 内联 prompt） | `opencode.json §agent` / `.opencode/agent/*.md` |
  | Claude Code | 根 `CLAUDE.md`（**只认这个名**） | `.claude/agents/*.md`（带 frontmatter `name`/`description`） |
  | Codex / AGENTS 标准 | 根 `AGENTS.md` | 不原生支持多角色 |
  | Cursor | `.cursor/rules/*.mdc` | 同上 |
- **「工具无关的角色定义」≠「自动识别」**：`CLAUDE.md` 等文件任何有文件读权限的工具都能读，但**能否被自动发现为「可选智能体」由各工具自己的注册入口决定**，与工程放什么文件无关。
- ⚠️ **已知待办（用户 2026-09-15 拍板「暂不处理」）**：**architect 侧在非 opencode 工具下必须显式声明**——因 `CLAUDE.architect.md` / `AGENTS.architect.md` **不是任何工具的默认加载名**（prototype 侧靠根 `CLAUDE.md`/`AGENTS.md` 撞对名而沾光）。若日后要跨工具使用，最小路径 = 建 `.claude/agents/{prototype,architect}-agent.md`。**此事实与 `agent/*.md` 存废无关**。
- **Claude Code 双概念需分清**：「自动读根 `CLAUDE.md`（项目上下文）」≠「自动发现有多个智能体（需 `.claude/agents/`）」。本工程两者皆缺，故在 Claude Code 下严格说**没有「可选智能体」概念**。
- **删除后连带的引用面（2 文件 4 处）**：`SETUP.md` 项目文件清单目录树（3 行）、`scripts/consistency-check.sh` 检查 4a/4b/5 的扫描目标列表（3 处）。`README.md` 已补更新日志。`archive/` 与 `.workbuddy/` 中的提及**属历史归档/日志，按约定不动**。

## 23. 模块间依赖：并入规范 §13 + 调用形态按部署分叉（2026-09-15 定型；v2.7）
- **`模块间接口依赖清单.md` 已删除**（模板）。删除依据：① **8 节无一独有**（§4 数据表归属 ↔ 规范 §5「表/所属模块」；§7 API 契约稳定性 ↔ 契约 §0.3 版本与兼容；§8 版本记录 ↔ 三层记录；§1/§2/§3 拓扑 ↔ 03 §1「关联模块」+ 03 §4）；② **`self-check` 对它零校验项**（唯一提及是路径深度表）→ 定性为**纯派生视图**，不应占独立文件形态。git 已跟踪，`git checkout HEAD -- <path>` 可恢复。
- **落点 = `系统级技术规范.md` §13「跨模块依赖」**（由**空壳 + 指针**升为**唯一权威源·汇总视图**）：§13.0 调用形态（按部署形态分叉）/ §13.1 模块依赖矩阵汇总 / §13.2 依赖关系图。**系统级文件 6→5**。
- **🔑 调用形态按部署形态分叉（核心口径，防系统性误报）**：
  | 部署形态 | 调用 | 锚点 | 与 `接口契约.md` 的关系 |
  |---|---|---|---|
  | **单体**（默认） | 本进程调用 | **业务能力**（如「校验客户是否存在」） | **不使用 `operationId`**——本进程调用不产生 HTTP 接口，契约中本无对应条目 |
  | **已拆服务** | 跨进程调用 | **`operationId`** | 须 ∈ `接口契约.md` §2 接口总表，并有 §3 契约明细 |
  部署形态未明确 → **按单体处理 + 登记 `TECH-OPEN-*`**（**不判失败**）；**不得为满足检查而臆造 `operationId`**。部署形态属**技术架构**，architect 不决策。
  > 由来：原 `self-check` 检查 1 硬性要求③「被调用接口须在契约 §3 存在」——在 **Spring Boot 单体**下模块间调用不产生 HTTP 接口，该表述会**系统性误报「凭空依赖」**。
- **§13 越界声明（写进文件，防回退）**：**不写容错 / 降级方案与同步·异步选型**（换栈会变 → 属技术架构）；业务侧异常处置语义在模块 `03` §2。
- **03 §4「跨模块依赖」**：加部署形态分叉引用块；指针改为 `../../系统级技术规范.md` §13（**不再指向已删文件**）。
- **`self-check` 检查 1** 由「跨模块契约核对」改为 **「跨模块依赖核对（按部署形态分叉）」**：单体只核对**被依赖模块存在且与 03 §1「关联模块」一致**；服务才核对 `operationId`；未明确按单体且**不判失败**。
- **依赖关系的记录面收缩**：此前被记**四遍**（03 §1「关联模块」/ 03 §4 / 规范 §13 / 依赖清单）→ 现为**三处且职责分明**：03 §1 记 FR-x 粒度、03 §4 记模块级来源、规范 §13 只做系统级汇总（**不新增事实**）。
- **「接口契约.md」保持独立（不做合并）**：它是**定义**（schema / 状态码 / 错误码，机器可读，可无损导出 `openapi.yaml`），依赖清单是**视图**；合并会毁掉导出能力与「前后端与测试共享边界物」的中立性。**定义 ≠ 视图**是本轮判定的主依据。
- ⚠️ **frontmatter / description 是独立残留面**：本轮 34 项断言全绿后，**全仓裸 grep 才暴露 `skills/tech-architecture/SKILL.md` 的 description** 仍列「模块间接口依赖清单.md」。→ 删改文件引用时**必须把 frontmatter 纳入扫描**，且**断言脚本不能替代全仓 grep 交叉验证**。

## 24. README / SETUP 日志写法：一天一条 + 只记结论（2026-09-15 定型）
- **权威源 = `archive/README.md` §三 第 4 条**（唯一）；`README.md` §修改日志 与 `SETUP.md` §更新日志 各只挂**一行引用注**，不复述细则。
- **一天一条**：同一天所有变更写入**同一条**，**不新增当日条目**；同日再次变更**就地合并**进当日条目（**废除**「上午 / 傍晚 / 夜 / 追加」时段后缀——旧写法曾把 2026-09-15 切成 7 条）。
- **结构**：首行一句话总述当天主线 → 其下按主题 **3–8 个要点**，每点 1–2 行。
- **只记结论**：保留结论与关键口径（边界判据 / 分叉口径 / 版本号），**删除论证 / 推导过程 / 被否决方案 / 逐文件清单**。职责边界：日志 = **最新变更摘要**；`archive/` = **完整过程文档**。
- **分工（消除双写）**：`README.md` 记**全量**变更摘要；`SETUP.md` **只记安装 / 配置 / 使用方式**相关变更（目录树 / 门禁说明 / 用法），其余指向 README。
- **回溯边界**：仅回溯合并 2026-09-15 当日（README 7 条 → 1 条 1341 字；SETUP 7 条 → 1 条 286 字）；**09-07 及更早条目保持原貌不动**（沿用「只归档不改写」谨慎原则）。
- ⚠️ **遗留（未处理）**：`SETUP.md` 的 09-07 / 09-04 / 09-02 三条内容仍属「全量类」，与新分工不符，待用户决定是否另行处理。
- 落地 3 文件：`archive/README.md`（规则）+ `README.md` + `SETUP.md`；`consistency-check.sh` **6/6 通过**。
