# 项目审计报告（2026-07-12 · 全新审计）

> 审计范围：CLAUDE.md / AGENTS.md / CLAUDE.architect.md / AGENTS.architect.md / opencode.json / templates/01-04 / 技术通用规则.md——全部以「零上下文」从头审读，不依赖前置结论。审计维度：**严谨性（逻辑闭口·无歧义） / 准确性（交叉引用·章节号·术语一致） / 简洁性（冗余·过度重复）**。

---

## 一、阻断级（须修复） 🔴

### F1. opencode.json architect prompt 阶段编号跳跃（0,1,2,3,4,7,8,9）

**位置**：`opencode.json` L20 architect-agent prompt

**问题**：编号为 `0,1,2,3,4,7,8,9`，4→7 断裂。原因是当初从"0+5阶段"缩为"0+3阶段"时删除了旧阶段 5/6（接口设计/模块详细设计），但未重编号。

**影响**：新 agent 看到编号跳跃会产生困惑，不知道 5/6 是否存在。

**建议修复**：`4→5, 7→6, 8→7, 9→8`，保持连续。

---

### F2. CLAUDE.md「Sibling Agent」节旧术语「接口规划」

**位置**：`CLAUDE.md` L327（Sibling Agent 描述）

**当前**：
```
产出技术架构/数据模型/接口规划
```

**应改为**：
```
产出技术规格（03）/ 数据模型（04）
```

**原因**：`接口规划` 是旧 05 文档名，05 已并入 03。同文件 L281（决策门禁）已正确使用「技术规格（03）/ 数据模型（04）」，此处是**同一文件内不一致**。

---

## 二、重要级（应修复） 🟠

### F3. 四层模型定义在 4 处重复

**位置**：`CLAUDE.md` §术语对照表（唯一权威）、`CLAUDE.architect.md` §你是谁、`AGENTS.architect.md` 角色设定、`opencode.json` 两个 prompt。

**问题**：同一概念在 4 处独立维护，更新时容易漂移。CLAUDE.md 已标注「唯一权威源，其他文件仅引用」，但 CLAUDE.architect.md / AGENTS.architect.md / opencode 实际是**全文复述**而非引用。

**建议**：CLAUDE.architect.md / AGENTS.architect.md 角色设定节改为引用式：
```markdown
> 📌 四层模型定义详见 `CLAUDE.md §术语对照表`（唯一权威源），此处不重复全文。
```
仅保留「本 agent 在第三层」一句定位。opencode.json 的 prompt 同理压缩为一行引用。

---

### F4. CLAUDE.md 核心约束 §4 与 §职责边界 重复

**位置**：`CLAUDE.md` L303-307（核心约束第 4 条）复述了 L57-62（§职责边界）的三类禁止项。

**问题**：同一禁止项在两处用不同措辞描述，存在漂移风险。L62 已声明「其他文件若需重申边界，仅写'详见 CLAUDE.md §职责边界'」，但核心约束自身违反了这个规则。

**建议**：核心约束第 4 条简化为：
```
4. 职责边界：需求包只描述产品功能与业务行为，**严禁包含技术实现定义**（接口/数据模型/技术选型）。详细边界见 §职责边界（唯一权威）。
```

---

### F5. AGENTS.md「进入执行流程」节与 CLAUDE.md §调用与决策 大量重复

**位置**：`AGENTS.md` L15-27

**问题**：整段是 CLAUDE.md §调用与决策 Step 0-2 的浓缩版。文件已声明 CLUADE.md 是「唯一权威编排逻辑」，但 AGENTS.md 仍在复述逻辑而非纯话术指引。若逻辑变更（如新增 Step），需同步两处。

**建议**：该节改为「话术指引 + 引用 CLAUDE.md」格式，不重复 Step 内容：
```markdown
## 进入执行流程（工程研判与归属判定）
> 判定逻辑唯一定义于 `CLAUDE.md §调用与决策` Step 0-2；本处仅提供话术指引。
```
然后仅保留「提醒用户选择放弃/更新」等**话术层面**内容，删除逻辑描述。

---

## 三、建议级（可优化） 🟡

### F6. CLAUDE.architect.md L19 措辞微调

**当前**：
```
- **阶段 0 + 3 阶段 + 门禁（4 道）**
```
**建议**：
```
- **阶段 0 + 3 阶段 + 4 道门禁**
```
更简洁直接。

---

### F7. CLAUDE.md「异常沟通指引」位置可优化

**位置**：`CLAUDE.md` L91-106，位于「版本规则判定标准」之后、「任务目录结构」之前。

**问题**：沟通指引属于「流程执行」层面，与「任务目录结构」更相关。当前位置夹在两个定义性章节之间，逻辑流断裂。

**建议**：移至「调用与决策」Step 3 之后，作为执行层面的操作指引。

---

### F8. 「prd-任务结果摘要」文件名过长

**位置**：`prd-任务结果摘要.md` / `tech-任务结果摘要.md`

**意见**：与简洁的 `03-技术规格说明书.md` 等文件名比，`prd-任务结果摘要.md` 过长。建议改为 `prd-结果摘要.md` / `tech-结果摘要.md`（前缀已区分来源）。

**但注意**：改名需同时更新 CLAUDE.md / CLAUDE.architect.md / AGENTS.md / 两个 delivery skill / 模板 / 工程摘要模板 中的引用，改动面大；是否执行取决于收益判断。本次仅提出观察。

---

## 四、验证通过项（无问题） ✅

| 验证维度 | 结论 |
|----------|------|
| 01 §6–§14 章节号体系（异常=§13、未决项=§14） | ✅ 模板正确，指令文件引用一致 |
| 02 弹窗二分原则（复杂→辅助页面[N-A]、简单→内联） | ✅ 模板 + AGENTS.md 一致 |
| 03 模块→功能点 9 子项编号与模板 | ✅ 一致 |
| 04「所属模块继承 03」 | ✅ 模板 + data-model skill 一致 |
| 技术通用规则 §1-§10 章节号 | ✅ 模板正确（错误码=§5, 分层=§7, 部署=§8, 非功能=§9, 变更=§10） |
| 功能组件 vs 模块 vs 代码模块 术语区分 | ✅ CLAUDE.md §术语对照表 + §关键术语澄清 清晰 |
| architect 核心约束⑨「生成中遇不确定暂停」 | ✅ 已从 FIX 落地于 4 处文件 |
| 门禁 0-3 对应 skill 与模板关系 | ✅ 正确 |
| 产品通用规则 vs 技术通用规则 职责分离 | ✅ 清晰 |
| 产品工程摘要 vs 技术工程摘要 镜像结构 | ✅ 对称 |
| FR-x 贯穿四层（01 功能 ≡ 02 功能组件 ≡ 03 功能点） | ✅ 定义一致 |
