# 项目审计报告（2026-07-13 · 增量审计）

> 审计范围：用户 7/13 新增 `self-check` skill + 修改 `CLAUDE.architect.md` / `AGENTS.architect.md` / `opencode.json`；7/12 修改的 `spec-analysis` / `tech-architecture` / `business-architecture` / `pencil-executor` skill 与 `agent/*`。审计维度：**严谨性 / 正确性 / 必要性 / 逻辑顺畅性**。

---

## 一、阻断级 🔴

### F1. `.opencode/skills/self-check` 符号链接缺失

**问题**：`skills/self-check/` 目录与 `SKILL.md` 已创建，但 `.opencode/skills/` 中没有指向它的符号链接。当前 `.opencode/skills/` 下共有 11 个链接，其中 `api-design` 和 `module-design` 悬空（指向已删除目录），而 `self-check` 完全不存在。

**影响**：`CLAUDE.architect.md` 核心约束① + 工作流程各阶段（L85/L92/L95）均在产出后**硬性要求先调用 self-check 再确认门禁**，但缺少符号链接会导致系统无法发现/加载该 skill——门禁前自检机制形同虚设。

**修复**：
```bash
cd .opencode/skills && ln -s ../../skills/self-check self-check
```

---

### F2. 两个悬空符号链接残留

**问题**：`.opencode/skills/api-design` 和 `.opencode/skills/module-design` 的链接目标目录（`skills/api-design/`、`skills/module-design/`）已于 7/12 删除，但符号链接未清理。

**影响**：系统在扫描 skill 时可能报错或产生不可预期的行为。且与 F1 一起看，`.opencode/skills/` 的状态与 `skills/` 不一致。

**修复**：
```bash
cd .opencode/skills && rm api-design module-design
```

---

## 二、重要级 🟠

### F3. `CLAUDE.architect.md` L13 旧措辞未修正

**位置**：L13

**当前**：
```
- **阶段 0 + 3 阶段 + 门禁（4 道）**：tech-common-rules(门禁0) → ...
```

**建议**（此为上轮审计 F6，未执行）：
```
- **阶段 0 + 3 阶段 + 4 道门禁**：tech-common-rules(门禁0) → ...
```

**理由**：「门禁（4 道）」冗余——"门禁"概念即含"道"的语义，中间插一个 `（4 道）` 不如合并为「4 道门禁」。

---

### F4. `self-check` 输出写入与「只追加」记录的语义张力

**问题**：self-check 定义将自检结果写入 `tech/tech-任务操作记录.md` 的「复核结果」字段——这是**修改已追加条目**，而该文件约定为「只追加」。

**评估**：实际流程是——spec-analysis 等 skill 先追加条目（含空「复核结果」），self-check 随后回填该条目的复核字段。逻辑上是合理的（复核是条目的组成部分，不是新操作），且 self-check 不配独立阶段输出独立文件（设计取舍正确）。

**建议**：在 data-model / tech-architecture / spec-analysis skill 的「记录」步骤中加一句：「复核结果」字段暂留 `待自检`，由后续 self-check 回填。明确告诉 agent：这不是"修改历史"，而是"补全条目的滞后字段"。消除「只追加」与「回填复核」之间执行者的歧义。

---

## 三、逻辑与必要性验证 ✅

### self-check skill 设计审查

| 维度 | 结论 |
|------|------|
| **必要性** | ✅ 强——两份审计报告（7/11、7/12）发现的章节号错位、旧术语残留、FR-x 追溯缺口等问题，人工复审成本高且易遗漏，自动化自检可堵漏 |
| **范围边界** | ✅ 精准——仅做三类检查（FR-x 追溯 / 场景覆盖 / 章节号+引用+术语），明确排除架构评审/合规/风险审核 |
| **触发时机** | ✅ 正确——门禁 1/2/3 前各一次，差异化范围（01↔02 → 01↔02↔03 → 全链路），门禁 0 不触发（无任务级产物） |
| **输出路由** | ✅ 正确——写入 `tech-任务操作记录.md` 的复核字段，不产出独立文档 |
| **门禁关系** | ✅ 清晰——「自检通过后再请用户确认门禁」，互补不替代 |

### self-check 集成验证

| 集成点 | 文件 | 状态 |
|--------|------|------|
| 核心约束① | CLAUDE.architect.md L125 | ✅ 「门禁 1/2/3 确认前须先调用 self-check」 |
| 阶段 1 流程 | CLAUDE.architect.md L85 | ✅ 「调用 self-check → 自检通过 → 门禁 1」 |
| 阶段 2 流程 | CLAUDE.architect.md L92 | ✅ 「调用 self-check → 自检通过 → 门禁 2」 |
| 阶段 3 流程 | CLAUDE.architect.md L95 | ✅ 「调用 self-check → 自检通过 → 门禁 3」 |
| 门禁 1 话术 | AGENTS.architect.md L71 | ✅ 「确认前须先调用 self-check 执行一致性自检」 |
| 门禁 2 话术 | AGENTS.architect.md L96 | ✅ 同上 |
| 门禁 3 话术 | AGENTS.architect.md L120 | ✅ 同上 |
| opencode prompt | opencode.json L20 | ✅ 引用 self-check |
| 关键文件引用 | CLAUDE.architect.md L147 | ✅ `skills/self-check/SKILL.md` |

_唯 spec-analysis SKILL Step 3 未提及 self-check——但这是合理的设计分层：skill 管产出、orchestrator（CLAUDE.architect.md）管门禁前步骤编排。不视为缺陷。_

### 上轮修复保持确认

| 修复项 | 文件 | 状态 |
|--------|------|------|
| F1: opencode 编号 | opencode.json | ✅ 0-7 连续 |
| F2: 接口规划 | CLAUDE.md | ✅ 残留 0 |
| F3: 四层模型 | CLAUDE.architect/AGENTS.architect/opencode | ✅ 均引用 |
| F4: 核心约束 §4 | CLAUDE.md | ✅ 1 行引用式 |
| F5: 进入执行流程 | AGENTS.md | ✅ 话术指引 |

### 7/12 修改 skill 状态

| Skill | 变更观察 |
|-------|---------|
| `spec-analysis` | +权威源引用（术语/话术/判据各归其源）、+精简 Step 3（话术委托 AGENTS.architect.md），逻辑更清爽 |
| `tech-architecture` | +权威源引用、功能点结构委托模板（避免 skill 与模板漂移） |
| `business-architecture` | 前置条件引用 CLAUDE.md §调用与决策（链路清晰），无功能变更 |
| `pencil-executor` | 未发生结构性变更（381 行·Pencil MCP 专属，不需要自检集成） |

---

## 四、执行建议

1. **立即修复 F1+F2**（两个 shell 命令，5 秒完成）。
2. **按需修复 F3**（措辞微调，1 行 Edit）。
3. **F4 为设计注释级**，不影响功能，可在后续 skill 微调时顺手补。
