---
name: data-model
description: 基于 03 技术架构，识别实体，定义业务表结构说明（表/字段类型与约束/建表语句DDL/索引语句DDL/关系/状态机/软删与分表约定），生成 tech/04-data-model.md 并经用户六维确认（门禁3：表/字段/建表语句/索引语句/表关联/字段状态机）。跨任务通用实体抽取进 ../技术通用规则.md；新通用标准项回写技术通用规则。
allowed-tools:
  - Read
  - Write
  - Edit
  - Glob
  - Grep
---

# 业务表结构 Skill — 阶段 3

## 输入
- `tech/03-tech-architecture.md`（已确认）
- 上游 01/02（功能与业务规则）
- `../技术通用规则.md`（命名/软删/审计约定）

## 前置条件
- tech-architecture 门禁2 已通过

## 输出
- `tech/04-data-model.md`（结构见 `templates/data-model.md`）

## 工作流程

### Step 1：实体识别
从功能模块与业务规则抽取实体（用户/订单/商品…），识别 1:N / M:N 关系，初步画出 ER 概要。

### Step 2：表与字段设计（含可执行 DDL）
逐表定义字段（类型/约束/默认值/索引），严格遵守 技术通用规则 命名与审计/软删约定；跨任务通用实体改为引用 技术通用规则 第 6 节，本文件只定义本期特有实体与字段。
每表**必须产出两份可执行 DDL**：`建表语句(DDL)`（CREATE TABLE，含主键/唯一/注释/引擎/字符集）与 `索引语句(DDL)`（CREATE INDEX / ALTER TABLE ADD INDEX），供编程智能体直接执行。DDL 须与上方字段表、索引表完全一致。

### Step 3：关系/状态机/分表
外键关联、事务边界、状态机（字段状态值/流转条件/触发事件）、分表分库规划（如适用）。

### Step 4：写出 04 并确认（门禁3 · 六维）
展示实体关系总览 + 核心表清单，并按**六维**逐表确认（门禁3）：
1. 表（清单/命名/所属模块）
2. 表字段（类型/约束/默认值/说明）
3. 建表语句（CREATE TABLE DDL 可执行、与字段表一致）
4. 索引语句（CREATE INDEX DDL 可执行、与索引表一致）
5. 表关联（外键/关系/事务边界）
6. 字段状态机（状态值/流转条件）
确认 → 进入 api-design（阶段 4）。

### Step 5：记录
追加 `tech/任务操作记录.md` 阶段 3 条目。
