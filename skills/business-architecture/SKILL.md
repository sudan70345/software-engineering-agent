---
name: business-architecture
description: 阶段 1 与用户沟通整体业务架构，生成系统级要求规范文档「系统要求规范.md」和系统级交互规范「系统交互规范.md」，为阶段 2 逐模块沟通打下基础
allowed-tools:
  - Read
  - Write
  - Edit
  - Glob
  - Grep
---

# 系统架构沟通 Skill — 阶段 1（新架构）

> **版本说明**：本 Skill 已升级为新架构，生成系统级要求规范 + 模块清单骨架 + 通用规则。
> 不再生成完整的 01/02 大文件，改为在阶段 2 按模块逐个生成小文件。

## 🛡️ 执行前自检（必做）

执行本 Skill 前必须确认：
1. ✅ 已读取项目级文件（项目级产品摘要.md + 通用异常处理.md + 通用校验规则.md）
2. ✅ 当前项目名与系统目录明确
3. ✅ 本 Skill 产出文件归属正确（prd/ 子目录）

**若任一项不确定 → 立即执行「CLAUDE.md §上下文恢复检查点」R1-R4 流程。**

---

## 术语说明（新架构）

| 旧术语 | 新术语 | 说明 |
|--------|--------|------|
| 工程 | 项目（project） | 避免与技术"工程"混淆 |
| 任务 | 系统（system） | 明确为可独立交付的子系统 |
| - | 模块（module） | 新增，文档最小单元（按页面/功能域拆分） |

---

## 输入

- 用户提供的需求描述（来自阶段 1 对话）

## 前置条件

- 项目级文件已存在：
  - `document/{项目名}/项目级产品摘要.md`
  - `document/{项目名}/通用异常处理.md`
  - `document/{项目名}/通用校验规则.md`
- 系统目录已创建：`document/{项目名}/{系统名}/`

---

## 输出

### 1. 系统级要求规范（新增）

**文件**：`document/{项目名}/{系统名}/prd/系统要求规范.md`

**内容**：
- §1 系统定位（一句话定位、系统边界）
- §2 业务场景总览（场景[Sx]清单）
- §3 角色与权限（本系统涉及的角色）
- §4 非功能需求（NFR）
- §5 **模块清单骨架**（模块名 + 对应页面 + 核心功能 + 所属场景）
- §6 范围与非目标

### 2. 通用规则（项目级 + 系统级）

**项目级**（`document/{项目名}/`，跨系统复用）：
- `通用异常处理.md`：全局异常、通用UI状态、操作反馈（Toast）
- `通用校验规则.md`：字段格式校验、通用长度校验、业务规则校验

**系统级**（`document/{项目名}/{系统名}/prd/`，系统内跨模块复用）：
- `系统交互规范.md`：按钮、弹窗、分页、表格、表单、导航的组件交互行为标准

**来源**：
- 从 `templates/prototype-agent/project-level/` 复制模板（通用异常处理.md、通用校验规则.md）
- 从 `templates/prototype-agent/system-level/` 复制模板（系统交互规范.md）
- 根据用户沟通补充本系统特有的通用规则；跨系统的异常/校验规则抽取至 `通用异常处理.md` / `通用校验规则.md`，角色/权限落系统级 `系统要求规范.md`、术语落 `项目级产品摘要.md` §0（见下文「通用规则维护」）

### 3. 系统说明（保持不变）

**文件**：`document/{项目名}/{系统名}/系统说明.md`

---

## 工作流程

### Step 0：前置检查

确认当前系统目录和文件归属：
- 系统目录：`document/{项目名}/{系统名}/`
- 输出目录：`prd/`（prototype-agent 独占）
- 本 Skill 负责生成：`系统要求规范.md` + `系统交互规范.md`（系统级）+ 项目级通用规则（通用异常处理.md / 通用校验规则.md，如不存在则创建）

### Step 1：系统级架构沟通

与用户沟通以下内容（参考 AGENTS.md 阶段1话术）：

#### 1a. 系统定位
「这个系统是什么？给谁用？解决什么问题？」
- 一句话定位
- 系统边界（与其他系统的关系、数据依赖、功能边界）

#### 1b. 业务场景总览
「用户在什么情境下要做什么？」
- 场景[S1]：场景名称 + 场景描述
- 场景[S2]：...
- 每个场景记录：涉及的角色、核心流程

#### 1c. 角色与权限
「哪些角色会使用本系统？」
- 从项目级约定（`项目级产品摘要.md` §0 通用术语表 / 已建系统的 `系统要求规范.md` §3）读取通用角色定义
- 确认本系统涉及的角色及其特有权限

#### 1d. 非功能需求（NFR）
「性能、安全、可用性有什么要求？」
- 性能：响应时间、并发量
- 安全：权限控制、数据加密
- 可用性：可用率、容错
- 兼容性：浏览器、设备

#### 1e. 模块清单骨架（核心）
「这个系统包含哪些页面/功能模块？」

**沟通方式**：
- 用户描述页面清单（主页面 + 辅助页面）
- 按页面边界划分模块：
  - **主页面[N]** → 独立模块
  - **辅助页面[N-A]**（复杂弹窗） → 归入所属主页面的模块
  - 简单弹窗 → 不独立为页面，归入主页面

**输出**：模块清单骨架表格

| 模块名 | 对应页面 | 核心功能 | 所属场景 | 状态 |
|--------|---------|---------|---------|------|
| 登录模块 | 页面[1] | 账号密码登录 | S1 | ⏸️ 待开始 |
| 商城首页模块 | 页面[2] + 页面[2-A] | 商品浏览、搜索、详情 | S1 | ⏸️ 待开始 |
| 购物车模块 | 页面[3] | 编辑购物车、预览小计 | S1 | ⏸️ 待开始 |

**字段说明**：
- **模块名**：功能域名称 + "模块"（如"登录模块"）
- **对应页面**：页面[N] 或 页面[N] + 页面[N-A]
- **核心功能**：一句话描述
- **所属场景**：场景[Sx]（可多个）
- **状态**：初始为"⏸️ 待开始"

#### 1f. 范围与非目标
「本期做什么，明确不做什么？」
- In-scope：本期范围
- Non-goals：明确不做的功能

### Step 2：生成通用规则

**判定标准**：
1. **跨系统复用**（全局异常、字段格式校验） → 项目级 `通用异常处理.md` / `通用校验规则.md`
2. **系统内跨模块复用**（按钮/弹窗/表格等组件交互行为） → 系统级 `系统交互规范.md`
3. **用户明确标注为「通用」** → 按上述层级归类

**操作**：
1. **项目级通用规则**（如不存在则创建）：
   - 从 `templates/prototype-agent/project-level/通用异常处理.md` 复制到 `document/{项目名}/通用异常处理.md`
   - 从 `templates/prototype-agent/project-level/通用校验规则.md` 复制到 `document/{项目名}/通用校验规则.md`
2. **系统级交互规范**：
   - 从 `templates/prototype-agent/system-level/系统交互规范.md` 复制到 `document/{项目名}/{系统名}/prd/系统交互规范.md`
3. 根据用户沟通和项目级通用规则，补充本系统特有的内容
4. 填充占位符（{项目名}、{系统名}、{YYYY-MM-DD}）

**产出文件**：
- `通用异常处理.md`（项目级）
- `通用校验规则.md`（项目级）
- `系统交互规范.md`（系统级）

### Step 3：生成 系统要求规范.md

**操作**：
1. 从 `templates/prototype-agent/system-level/系统要求规范.md` 复制模板到 `document/{项目名}/{系统名}/prd/系统要求规范.md`
2. 填充以下内容：
   - §1 系统定位（来自 Step 1a）
   - §2 业务场景总览（来自 Step 1b）
   - §3 角色与权限（来自 Step 1c）
   - §4 非功能需求（来自 Step 1d）
   - §5 模块清单（来自 Step 1e，**骨架版**，仅基本信息）
   - §6 范围与非目标（来自 Step 1f）
3. 填充占位符（{系统名}、{项目名}、{YYYY-MM-DD}）

**注意**：
- §5 模块清单此时仅为骨架，状态均为"⏸️ 待开始"
- 阶段 2 逐模块沟通时，每完成一个模块，回来更新该模块的状态为"✅ 已完成"

### Step 4：展示给用户确认（确认门禁1）

展示系统要求规范摘要（参考 AGENTS.md 确认门禁1）：

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 系统架构概览（阶段1完成）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

系统名：{系统名}
定位：{一句话定位}

业务场景：
  - 场景[S1]：{场景名称}
  - 场景[S2]：{场景名称}
  - ...

模块清单（{N}个模块）：
  - 登录模块（页面[1]）
  - 商城首页模块（页面[2] + 页面[2-A]）
  - 购物车模块（页面[3]）
  - ...

通用规则：
  ✓ 通用异常处理.md（项目级，已生成）
  ✓ 通用校验规则.md（项目级，已生成）
  ✓ 系统交互规范.md（系统级，已生成）

──────────────────────────────────────────
下一步：阶段2 逐模块沟通，为每个模块生成详细的需求规格和UI规格。

请确认：
1. 系统定位是否准确？
2. 模块划分是否合理？
3. 是否有遗漏的模块？
```

**用户反馈**：
- 提出修改 → 更新 `系统要求规范.md`，重新确认
- 确认 → 推进到阶段 2（调用 page-specs Skill）

---

## 通用规则维护（项目级 + 系统级公共规则抽取）

**触发条件**：同一规则/术语/标准在≥2 个模块或系统中出现，或用户明确标注为「通用」

**抽取内容与落点**：
- 异常处理 / 通用UI状态 / 操作反馈 → 项目级 `通用异常处理.md`
- 字段格式 / 长度 / 业务校验 → 项目级 `通用校验规则.md`
- 角色与权限 → 系统级 `系统要求规范.md` §3
- 组件交互行为 / UI 标准 → 系统级 `系统交互规范.md`
- 业务术语 → 项目级 `项目级产品摘要.md` §0 通用术语表

**抽取动作**：将通用内容写入上述对应文件，系统/模块文档内改为引用

**维护时机**：新建项目时用于初始化；已有项目时增量补充/修订

> 📌 详细抽取判据与流程详见 `CLAUDE.md §项目公共规则`

---

## 输出文件清单

阶段 1 完成后，应生成以下文件：

```
document/{项目名}/
├── 项目级产品摘要.md            ← 项目级全貌（含通用术语表）
├── 通用异常处理.md              ← 项目级通用异常
├── 通用校验规则.md              ← 项目级通用校验
└── {系统名}/
    ├── 系统说明.md
    └── prd/
        ├── 系统要求规范.md       ← 新架构核心
        ├── 系统交互规范.md      ← 系统级交互规范（组件交互行为标准）
        └── modules/
```

**不生成**（留待阶段2）：
- ✅ 阶段2按模块生成：`modules/{模块名}/01-{模块}-需求规格.md` 和 `02-{模块}-UI交互规格.md`

---

## 注意事项

1. **模块清单骨架**：阶段1只生成模块清单骨架（模块名+页面+核心功能），详细规格在阶段2生成
2. **通用规则**：从模板复制后，根据用户沟通补充本系统特有规则
3. **确认门禁**：用户确认系统要求规范后，才能进入阶段2
