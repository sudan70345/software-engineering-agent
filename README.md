# prototype-agent
这是一个 Agent，可以自动分析用户产品需求，生成结构化的产品需求文档（需求包）并存放本地。
在用户明确同意后，它还可以可插拔地调用本地 Pencil MCP 生成原型，但原型生成是可选的下游能力，并非主流程。

## 运行流程简述（双段式）

1. **第一部分 · 需求分析（必做）**
   - 阶段 1：沟通业务架构，生成 `01-business-architecture.md` 并确认。
   - 阶段 2：逐页面沟通规格，生成 `02-page-specs.md` 并确认。
   - 两份文档合称「需求包」，仅描述产品功能与页面结构，不绑定任何绘图工具。

2. **决策门禁（需求包确认后触发）**（判定逻辑与结果处理详见 CLAUDE.md「调用与决策」）
   - 需求包确认后，agent 询问用户是否继续后续任务。
   - 当前仅提供「原型生成（Pencil 方案）」。
   - 用户拒绝 → 流程结束（任务结果摘要.md 已随需求包定稿生成、任务操作记录.md 已追加、工程摘要.md 已刷新）。
   - 用户同意 → 提示打开本地 Pencil 桌面应用并确保 Pencil MCP 服务已启动，进入第二部分。

3. **第二部分 · 原型生成（可选 · 当前绑定 Pencil）**
   - 仅在决策门禁中明确同意后调用 `skills/pencil-executor`。
   - 产出 `.pen`、截图、自校验报告、交付摘要。

4. **主要输出目录（工程项目 / 任务 两级）**
   - `prototype/{工程名}/{任务名}_{YYYYMMDD}/`
   - 工程级：`prototype/{工程名}/工程摘要.md`（全任务索引）、`prototype/{工程名}/project-common-rules.md`（跨任务共享，角色/权限/通用标准/术语）
   - 需求包（必出）：`01-business-architecture.md`、`02-page-specs.md`
   - 记录文件（必出）：`任务结果摘要.md`、`任务操作记录.md`
   - 原型包（可选）：`{task_name}.pen`、`pencil-execution-log.md`、`screenshots/`、`screenshots-reviewed/`、`validation-report.md`、`delivery-summary.md`、`ui-design-elements.md`

## 关键约束

- 工程项目归属：任务按业务归属归入 `prototype/{工程名}/`；同工程任务共享 `project-common-rules.md`，须读取、遵守并维护。
- 工程公共规则跨任务共享：角色定义、权限关联、通用业务规则、UI 标准与术语沉淀于 `prototype/{工程名}/project-common-rules.md`；收集过程中发现可通用规则须及时抽取进该文件。第二部分原型生成亦须遵循。
- 需求包工具无关：禁止写入 Pencil / 绘图工具的实现细节。
- 工程链路：本 agent 产出需求包（上游产品功能基础说明）→ 技术架构 agent 生成功能设计文件 → 编码 agent 生成代码。本 agent 不负责技术设计。
- 原型生成是可插拔方案：当前仅 Pencil；调用前必须提示用户打开本地 Pencil 桌面应用并启动 Pencil MCP 服务。
- 仅创建一个 `.pen` 文件；主页面和弹窗都应在同一文件内的独立 Pencil 页面中。
- 颜色必须使用 `#RRGGBB`，字体声明必须使用字符串字面量。
- 在进入原型生成前，必须先完成决策门禁，用户没有明确同意则不执行原型生成。

## 修改日志

- **2026-07-08**：确立双段式工作流（需求分析必做 + 原型生成可插拔）与三阶段工程链路（需求包→技术架构→编码）；引入工程项目层级（`prototype/{工程名}/{任务名}_{YYYYMMDD}/`）与三层记录体系（工程摘要 / 任务结果摘要 / 任务操作记录，当前态与历史分离）；收口单一权威源（CLAUDE.md 统一定义决策门禁、工程归属判定与原型约束索引，其余文件仅引用）；清理原型编号四套词汇与 Pencil 约束三重定义，补 02 未决项字段与三层文件模板。`prototype/` 下历史目录维持现状不动。
